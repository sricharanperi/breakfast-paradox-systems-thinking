# MASTER CONTEXT — Breakfast Paradox / Systems Thinking Capstone

**This file is the single source of truth for this repository's history, decisions, research, technical pipeline, and open threads.** It exists so that any person or any AI agent (Claude or otherwise, on any account) can pick this project up cold and have full context — no prior chat log required.

---

## 0. HOW TO MAINTAIN THIS FILE (read this first, every time)

- **Update this file after every significant piece of work** — a new deliverable, a bug fix, a research finding, a pipeline change, a decision the user made. "Significant" means: if it would matter to someone resuming this project from scratch, it goes in here.
- **Append, don't rewrite.** Add new dated entries to the `SESSION LOG` (Section 4). Only edit earlier sections (People, Repo Map, Playbook, Open Items) when the underlying fact actually changed — e.g. a new tool became available, an open question got answered.
- **Every session entry should cover:** what the user asked for, what inputs/data the user gave, what was investigated, every decision made and *why*, every mistake hit and how it was found/fixed, what was produced, and what's left open.
- **When an open item (Section 6) gets resolved, move it** from "Open Items" into the session log entry that resolved it, and delete it from Section 6.
- **This file is committed and pushed to `origin/main`** (the user's explicit choice — see Session 2026-09-12, decision D-14). That is a deliberate departure from this repo's usual practice of scrubbing AI-pipeline fingerprints from submitted deliverables (see Section 5, "AI-fingerprint scrubbing"). This file itself is *not* scrubbed — it is allowed to say "Claude," "pandoc," "tectonic," etc. freely, because it lives outside `deliverables/` and is not itself submitted for grading. If that assumption ever changes, flag it to the user before continuing to write AI-process detail into a tracked file.
- **Do not blindly trust old entries as still true of the live repo.** Before recommending or reusing anything referenced below (a file path, a script, a tool flag), verify it still exists — treat this file the way the harness's own memory-system guidance treats memory: a snapshot in time, not a live oracle.

---

## 1. PROJECT OVERVIEW

**Course:** Systems Thinking, Semester 3, PDM program (per `Courses-Syllabus_M26-V1.pdf` in the parent folder).

**Project name:** "Breakfast Paradox" — a systems-thinking capstone studying why students at IIIT Hyderabad register for the hostel breakfast mess in large numbers but a large share don't actually show up to eat, despite billing being tied to registration, not attendance.

**Repository:** `https://github.com/sricharanperi/breakfast-paradox-systems-thinking` (git repo lives at `/Users/Shared/Files From e.localized/PDM/Sem3/Systems Thinking/Project`; the parent `Systems Thinking` folder holds this repo plus unrelated course materials — lecture slides, the syllabus, reference textbooks — that are **not** part of the git repo).

**Team submission name:** "Invictus" (per the final merged-PDF filename, `Invictus_Phase1.pdf`).

**Deliverable structure (Phase 1, complete):**
| # | Deliverable | Markdown source | Docx | PDF |
|---|---|---|---|---|
| 1 | System Context Brief | `01-system-context-brief.md` | `System Context Brief.docx` | `System Context Brief.pdf` |
| 2 | Stakeholder Map | `02-stakeholder-map.md` | `Stakeholder Map.docx` | `Stakeholder Map.pdf` |
| 3 | Power-Interest / Leverage Map | `03-power-interest-leverage-map.md` | `Power-Interest Leverage Map.docx` | `Power-Interest Leverage Map.pdf` |
| 4 | Process Trace | `04-process-trace.md` | `Process Trace.docx` | `Process Trace.pdf` |
| 5 | System Timeline / BOT Map | `05-system-timeline-bot-map.md` | `System Timeline BOT Map.docx` | `System Timeline BOT Map.pdf` |
| — | Consolidated Report | `00-consolidated-phase1-report.md` | `Phase 1 Consolidated Report.docx` | `Phase 1 Consolidated Report.pdf` |
| — | Final submission bundle | — | — | `Invictus_Phase1.pdf` (straight concatenation of the five deliverable PDFs, 42 pages, no re-summarization) |

All six live in `deliverables/phase-1/`, alongside `assets/` (mermaid `.mmd` diagram sources + rendered `.png`s) and a set of `prathyusha_*.md` files (a teammate's raw/working documents, kept as historical record rather than deleted once consolidated).

**Deliverable structure (Phase 2, in progress):**
| # | Deliverable | Markdown source | Docx | PDF |
|---|---|---|---|---|
| 6 | System Map (+ Causal Loop/Feedback Analysis) | `06-system-map.md` | `System Map.docx` (21pp) | `System Map.pdf` (21pp) |
| 7 | Systemic Problem Analysis | `07-systemic-problem-analysis.md` (single consolidated file, merging Neha's original draft with the full rebuild — see 2026-09-13 "final consolidation" session below) | `Systemic Problem Analysis.docx` | `Systemic Problem Analysis.pdf` |

Both live in `deliverables/phase-2/`, with their own `assets/` (some newly drawn, some reused/copied from `deliverables/phase-1/assets/` — see the two 2026-09-12 Phase 2 session entries below). Both Task 6 and Task 7 are now fully built end-to-end (markdown, docx, PDF) as of the 2026-09-13 "final consolidation" session, which also replaced the LaTeX/tectonic PDF pipeline with a new HTML+CSS+Chrome-headless-print-to-pdf pipeline for tighter layout (see Section 5.7). Task 7 exists as **one** file going forward — `prathyusha_task7.md` (the interim, more rigorous but scaffolding-heavy rebuild) was merged into `07-systemic-problem-analysis.md` and removed; the full research-grade detail (evidence audits, validation-gap Q&A, traceability registers) that used to live in that interim file's prose still lives in `deliverables/phase-2/assets/task7/*.md`, just no longer duplicated into the polished narrative.

There is also a `report/` directory with a separate LaTeX-based write-up (`report/main.tex` → `report/main.pdf`) and its own `report/figures/` — a parallel, more traditional academic-report rendering of the same diagrams, distinct from the `deliverables/phase-1/*.docx→pdf` submission pipeline.

---

## 2. PEOPLE

| Git identity | Real name (as committed) | Role in this repo |
|---|---|---|
| `sricharanperi` <sricharanperi27@gmail.com> | (the user of this Claude Code session) | Repo owner; System Context Brief author; Phase 1 consolidation, docx→PDF conversion pipeline, final submission prep |
| `Kalluri Lakshmi Prathyusha` <klprathyusha@Kalluris-MacBook-Air.local> (commits through 2026-09-12) / `Prathyusha Kalluri` <87800998+PrathyushaKalluri@users.noreply.github.com> (GitHub-linked identity, used from 2026-09-13) | Prathyusha | Teammate; Stakeholder Map, Power-Interest/Leverage Map, Power Analysis Brief, governance research (Student Parliament, Mess Committee/Office), interview guides 1 & 3, most of the primary interview fieldwork, braindumps #1 and #2 (CFS/CDS interview, Kadamba kitchen visit, mess posters). **Same person, two different git identities** — the second is her actual GitHub account (`PrathyushaKalluri`, verified via `gh api user`), configured as this repo's local `user.name`/`user.email` starting 2026-09-13 so future commits attribute to her GitHub account rather than a bare local-machine identity. |
| `neha771` <nehasusan369@gmail.com> | Neha | Teammate; interview guide contributions, independent interview-transcript summary/cross-check, report figure assets; authored Phase 2 Tasks 6-7 (System Map, Systemic Problem Analysis) solo |

All three commit to the same `main` branch directly (no PR workflow observed in this repo — merges are plain `git merge` after concurrent pushes).

---

## 3. REPOSITORY / ENTITY MAP

```
Project/                                   (git repo root)
├── MASTER_CONTEXT.md                      ← this file
├── .gitignore                             (excludes copyrighted book PDFs + OS cruft)
├── deliverables/phase-1/                  Submission-ready outputs (the graded artifact)
│   ├── 00-consolidated-phase1-report.md … 05-system-timeline-bot-map.md   (markdown source of truth for each deliverable's prose)
│   ├── *.docx                              Word versions, hand-finished (diagrams manually placed, occasional manual edits post-generation)
│   ├── *.pdf                               Submission format, generated FROM the .docx via pandoc+tectonic (see Section 5 playbook)
│   ├── Invictus_Phase1.pdf                 Final bundle: straight concat of the 5 deliverable PDFs
│   ├── prathyusha_*.md                     Prathyusha's raw working documents (superseded by the consolidated .md files above, kept for provenance)
│   └── assets/                             Diagram sources (.mmd = Mermaid source) and rendered .png exports, used both in the docx files and report/
├── deliverables/phase-2/                  Phase 2 outputs — Task 6 (System Map) complete incl. docx/PDF; Task 7 still in progress
│   ├── 06-system-map.md                    Task 6: System Map (actors/process/resources/constraints, category map, service blueprint, CLDs — synthesis of Phase 1, one new method: Service Blueprint) — DEEPENED 2026-09-13, see Section 4
│   ├── System Map.docx / .pdf              Task 6's submission-ready output
│   ├── 07-systemic-problem-analysis.md     Task 7: Systemic Problem Analysis (events→patterns→structures→mental models, feedback loops, root causes) — NOT YET updated to this session's visual style; still Mermaid (see Section 9)
│   └── assets/                             diagram1, diagram3-diagram13: hand-composed SVG built via the `systems-visual-design` skill (see Section 5.6), each with its `gen_*.py` generator source kept alongside its `.png` (diagram10/11 = B3/R3 loops, diagram12 = CDS/CFS governance, diagram13 = kitchen/supply subsystem, all added 2026-09-13); diagram2-underprovisioning-loop.mmd/.png (Task 7's, still Mermaid) is the only diagram left in the old style; plus 4 photographed CDS/CFS posters added 2026-09-13 (`WhatsApp Image 2026-09-13 *.jpeg` — CDS Student Handbook, CDS Food Card User Guide, main CDS overview poster, FSSAI Eat Right Campus certificate)
├── .claude/skills/systems-visual-design/  This project's diagram toolkit: SKILL.md, svgkit.py (shared SVG component library), icon-defs.svg (hand-drawn icon set) — use for every diagram from now on, see Section 5.6
├── report/                                 A separate, parallel LaTeX academic write-up of the same project
│   ├── main.tex / main.pdf
│   └── figures/                            Its own .mmd + .png diagram set (numbered d1–d14, overlaps conceptually with deliverables/phase-1/assets but is a separate copy)
├── research/                               All research inputs and methodology notes (NOT submitted directly, but everything above is built from this)
│   ├── primary-research/
│   │   ├── interviews/                     Real interview transcripts (the project's core evidence base)
│   │   ├── screenshots/                    Portal screenshots (registration UI, capacities, rates) — filenames are dated by the calendar date shown IN the screenshot's content, not necessarily the capture date
│   │   ├── team-contributions/             Teammate-authored research notes folded into the main brief
│   │   └── index.md
│   ├── methodology/                        Interview guides (1, 2, 3), synthetic pilot-testing notes, UX-research-method reading notes
│   ├── working-drafts/                     Superseded early drafts, kept as historical record
│   ├── dt-worksheets/                      Design Thinking Toolbox worksheet templates (vendored reference material)
│   ├── service-design-playbook/            Vendored reference material (license + README only tracked)
│   ├── book-notes-fifth-discipline*.md     Reading notes on Senge's "The Fifth Discipline" / Fieldbook
│   ├── course-and-syllabus-study.md
│   └── tool-notes.md
├── tools/                                  Vendored open-source diagramming tools (Loopy, a CLD editor, a stakeholder-map tool) — reference/inspiration, not built or run as part of the pipeline
├── project_framework .pdf                  The assignment's own methods framework — deliverable section headers are written to match this document's wording exactly (a deliberate, repeated decision — see D-2 below)
├── prathyusha_braindump.md                 Prathyusha's braindump #1 (2026-09-05) — pre-structured raw notes
├── prathyusha_braindump_2.md               Prathyusha's braindump #2 (2026-09-13) — CFS/CDS interview + Kadamba kitchen visit + mess poster transcriptions
└── (large reference textbook PDFs — gitignored, not redistributed)
```

**Key cross-references (entity relationships worth knowing):**
- Every `deliverables/phase-1/0N-*.md` file's section headers are written to match `project_framework .pdf`'s own activity/method wording verbatim — a standing constraint, not a one-off choice.
- Every `deliverables/phase-1/0N-*.md` traces back to specific `research/primary-research/interviews/*.md` entries and `research/methodology/*` guides; the "Outstanding Data Collection" section at the end of Tasks 3–5 explicitly lists which claims still need mess-committee/vendor-side data that hasn't been collected yet.
- `deliverables/phase-1/assets/*.mmd` and `report/figures/*.mmd` are Mermaid diagram sources rendered to `.png` for embedding — these are two separate copies of conceptually overlapping diagrams (one set feeds the Word/PDF deliverables, the other feeds the separate LaTeX `report/`).
- `prathyusha_*.md` files in `deliverables/phase-1/` are raw inputs that were manually folded into the numbered `0N-*.md` files by sricharanperi in later commits; they are intentionally not deleted (explicit decision, see commit `312b663`: "Mark context-brief corrections file as superseded... kept as historical record rather than deleted").

---

## 4. SESSION LOG (chronological, most recent last)

> Each entry below covers one working session (one Claude Code conversation). Sessions before 2026-09-12 are reconstructed from git commit messages, diffs, and file timestamps — no original chat transcript survived for them, but by the user's explicit instruction (2026-09-12 session, decision D-13) they are written with the same level of narrative detail as a directly-observed session, not flagged as lower-confidence.

### Session: 2026-09-03 — Project bootstrap
**Commit:** `dbf9247` "Initial commit: Breakfast Paradox systems-thinking capstone project"
**By:** sricharanperi, with Claude (`session_019kay9AyBmu5aLpaiUKTt3M`)

- First commit of the whole repo: course materials, `project_framework .pdf`, primary research (first interview, portal screenshots), the first draft of the System Context Brief, research methodology notes, and the vendored diagramming tools (Loopy, CLD editor, stakeholder-map tool).
- **Decision D-1:** Copyrighted commercial book PDFs (Buxton, Carroll, Becker, Constantine, Hall, Tidwell, etc.) are excluded from git via `.gitignore`, even though they were used as research input — "not ours to redistribute." This is the origin of the current `.gitignore`.

### Session: 2026-09-05 (morning) — Prathyusha: synthetic pilot interviews
**Commits:** `7453b41` (braindump), `9c4ca4d` (synthetic pilot + Guide 1 rewrite), `600e009` (progress plan)

- Prathyusha ran two AI-simulated personas (an "attender" and a "skipper") through draft Interview Guide 1 to stress-test the questions before real interviews were conducted.
- A third AI critique pass on those synthetic transcripts caught 4 leading/binary questions in the guide and surfaced new angles worth asking about: batch weekly registration, a "companion-absence effect" (does a friend not attending change your own decision), and skepticism toward proposed levers/interventions.
- **Decision D-2:** Guide 1 was rewritten based on this critique. The synthetic transcripts and the critique log were kept but explicitly marked **non-ground-truth** — a recurring, important discipline in this project: synthetic/simulated data is never allowed to silently masquerade as real respondent evidence. (This discipline is why, later on 2026-09-06, the "synthetic pilot" methodology references get scrubbed from the Power Analysis Brief once real data supersedes them — see that session.)

### Session: 2026-09-05 (afternoon/evening) — sricharanperi: System Context Brief overhaul + Prathyusha's Activity 2
**Commits:** `a967e3a`, `4f65c2c`, `325974f`, `45254b1` (merge), `5da4d00`, `a5f7a4c`, `00c5489`, `bc4dc25` (merge), `312b663`, `20d21eb`
**By:** sricharanperi with Claude (`session_01UURzEtfrZ3EbRSeQgXD8kp`), interleaved with Prathyusha's own commits (`session_01UwZvjo2cHAtuzXRd1NgoyS`)

This was a big, multi-pass session. Key decisions and work:

- **Decision D-3:** Split the System Context Brief's "Purpose" section into three: true purpose (2a), the actual operating mechanism (2b), and the gap between them (2c). Rationale: the registration/billing mechanism rewards pre-committing over actually attending, which the team judged explains more of the observed paradox than the previously-assumed "class-schedule overlap" explanation alone.
- Integrated a teammate's independent hypothesis (Skip Meal vs. Mess Cell as a dominant strategy for students), added behavioral-economics lenses (loss aversion, present bias, sunk-cost numbing, default effects) and 8 research verticals, archiving the teammate's verbatim contribution under `research/primary-research/team-contributions/` rather than paraphrasing it into the main text — **decision D-4: attribution and traceability of teammate research is preserved by keeping their verbatim file, not just their conclusions.**
- Added a fifth persona ("Role E — rush/latecomer") and revised bias-loaded questions.
- Read and incorporated UX-research methodology texts (Erika Hall's *Just Enough Research*, Nunnally & Farkas, Dumas & Loring, King/Churchill/Tan) into the project's own interview standard (Section 11 of the Context Brief).
- Reconciled sricharanperi's independent Context Brief rewrite with Prathyusha's parallel Phase 1 work (which had been developing Stakeholder Map, Power-Interest Map, Process Trace, System Timeline, and Guide 3 concurrently) — fixed one stale cross-reference that the merge introduced.
- **Decision D-5:** Rewrote the Context Brief to match `project_framework .pdf`'s Activity 1 wording exactly, in the framework's own order, and **removed all hedging language, source citations, and process narration** in favor of direct factual statements — the working draft with that narration was archived to `research/working-drafts/`, not deleted. This "state findings directly, no visible research-process narration, no citations" style is a standing decision that shows up again and again in later sessions.
- Prathyusha's Activity 2 (Stakeholder Map) work: corrected mess structure from a veg/non-veg framing to a cuisine-based one (Kadamba=South Indian, Bakul/Palash=North Indian, Yuktahar=Jain); split a previously-collapsed "Mess Committee/Mess Office" into distinct governance actors; discovered Ping (an informal/unofficial coordination tool) and portal-IT as stakeholders; replaced a weak "Bakul is newer" explanation for its low uptake with a stronger cuisine-preference hypothesis, evidenced across three messes.
- sricharanperi then integrated those corrections into the Context Brief: cuisine-based mess framing; governance split into Mess Committee (policy, faculty-chaired — *later found to need softening, see 2026-09-06*), Mess Office (operations, ~4-day procurement lead time, issuer of a workload-driven cancellation-policy change), and Warden (vendor management); corrected the escalation-mechanism symptom from "no channel exists" to "three channels exist, none with a confirmed record of producing policy change"; rewrote the Initial Problem Framing into short plain sentences ending in one direct question.
- Prathyusha then **consolidated** the Stakeholder Map: merged a diagram-first map and a separate stakeholder-register file into one document (every stakeholder now carries role/interest/need/expectation in prose, not split across files) — **decision D-6, a repeating pattern in this project: raw/working files get merged into one polished, submission-shaped document, and the redundant intermediate file is deleted with cross-references fixed**, except where explicitly kept for provenance (see D-4, and `312b663` marking a corrections file "superseded... kept as historical record").
- sricharanperi then built the final `02-stakeholder-map.md` + `.docx`, consolidating Prathyusha's `prathyusha_02-stakeholder-map.md` into the **Design Thinking Toolbox's own stakeholder-map template structure** (Use Case, Stakeholders, Create the Map, Relationships, Findings) — **decision D-7: deliverable structure follows the assignment's method-toolbox templates, not an ad hoc structure**, covering all four of Activity 2's required methods: primary/secondary/indirect classification (17 stakeholders included, 4 minor, 3 excluded — explicitly quantified), a full interests/needs/expectations table, a relationship/dependency/conflict matrix, and formal-vs-informal role distinctions. Added three new diagrams (a DT-template concentric-ring map, a relationship-network view, an onion/proximity-to-student view), with the source PowerPoint template kept in `assets/` for provenance.

### Session: 2026-09-06 (early) — neha771: interview guide + report assets
**Commits:** `0f07e6e`, `9f865e9`

- Neha contributed a "quick interview guide" and some in-progress report assets. (No Claude co-authorship trailer on these two commits — likely done without an AI session, or without the trailer being added.)

### Session: 2026-09-06 (mid-afternoon) — Prathyusha: Activity 3 (Power-Interest/Leverage Map)
**Commits:** `84e16d2`, `46d5196` (merge), `c15a748`, `b939e4c`, `6cd091a`
**By:** Prathyusha with Claude (`session_01UwZvjo2cHAtuzXRd1NgoyS`)

- Built a full first-principles power/leverage analysis from scratch: a power taxonomy, a 26-domain decision-rights matrix, formal-vs-informal power tracing, five incentive chains, bargaining/dependency asymmetry analysis, historical precedent research, resource-control mapping, and leverage-rights analysis.
- **Key findings this pass:** Mess Committee / Mess Office / Warden are three separate governance actors with an *unconfirmed* relationship between them; Academic Administration has zero relationship to any of the three (confirmed three independent ways); students control the one input (registration intent) every other actor's forecasting depends on, while bearing no cost for its inaccuracy — a central systemic tension of the whole project; students' collective organizing capacity (proven to exist via the informal Mess Cell resale market) has never been observed aimed at governance rather than peer-to-peer transactions — termed "sleeping leverage" at this point.
- **Decision D-8:** Ten open questions were logged and explicitly left unanswered rather than guessed at, with raw discovery notes kept separate from the polished brief — a discipline of not inventing certainty maintained throughout the project.
- A follow-up commit (`c15a748`) folded answers to those ten questions back in. Two answers **revised** findings rather than just filling gaps: (1) a confirmed student representative on the Mess Committee softened the "consulted but cannot decide" finding; (2) a confirmed prior instance of organized student action aimed at governance (not just peer-to-peer) overturned "sleeping collective leverage," relabeling it **"exercised-but-unmeasured leverage"** since the subject/outcome of that action was still unknown. Also surfaced a new term, "Mess Council," as the confirmed recipient of a public complaint-email channel, with an unresolved relationship to the Mess Committee (this ambiguity is later mostly resolved — see next session).
- A synthetic pilot transcript for Guide 3 (governance) was logged (`b939e4c`), explicitly flagged as **weaker evidentiary status** than the student-side synthetic pilots, since no real governance-side account existed anywhere in the project yet to validate the persona against. Its stated purpose was only to check that the Guide 3 questions elicit honest uncertainty rather than manufactured confidence — not to produce findings.
- A full audit pass (`6cd091a`) self-scored the Power-Interest/Leverage Map + Power Analysis Brief at "75/100, Strong not yet distinction-level," logged a completion plan for every gap, and applied P0/P1 fixes directly: corrected evidence-confidence drift on the "faculty-chaired" Mess Committee claim, added false-precision disclaimers to both quadrant charts, added a precedent-honesty statement, added a domain-specific "Authority at a Glance" table, flagged two incentive chains as hypothesis-only.
- **Independent research finding (verified from source, not a search snippet):** IIIT-H's Student Parliament maintains an elected Mess Secretary and per-mess Mess Deputy Secretaries — a real, reachable stakeholder that had been entirely missing from the Stakeholder Map. Added there. Also found that "Mess Council" was very likely a naming mix-up with a *different* institution (IIT Hyderabad, not IIIT-H) — an important correction, not a new confirmed actor. Found two new historical precedents ("Let Them Eat Frogs," "Water Mess") showing a recurring "complaint ignored until crisis" pattern, and a strong-but-unconfirmed candidate match for the previously-unidentified organized student email effort. The interview guide was updated to prioritize the Mess Secretary as the single best remaining research target.

### Session: 2026-09-06 (evening) — neha771 + Prathyusha: real interview data, Activities 4 & 5
**Commits:** `5eedfbf` (merge), `cdb36c0`, `edc55e6`, `1f074ae`
**By:** Prathyusha with Claude (`session_01UwZvjo2cHAtuzXRd1NgoyS`), plus neha771's independent transcript work

- Five real student interviews were conducted and logged (2 daily eaters, 3 skippers) — `research/primary-research/interviews/2026-09-06_five-student-interviews-guide1.md` — giving Activities 4 (Process Trace) and 5 (System Timeline/BOT Map) their first real evidentiary base. Both were explicitly split into an "A" part (established now, from real data) and a "B" part (a completion plan for the entirely-unobserved mess/kitchen side) — **decision D-9, same discipline as D-8: don't claim what you haven't observed, and say explicitly what's still missing.**
- **Key findings:** monthly (not weekly) batch registration confirmed directly by respondents; Skip Meal shows **zero real usage** across every respondent who addressed it — strong confirmation of the dominant-strategy hypothesis; peer influence was **disconfirmed** as a decision-determining factor and downgraded to a weak motivator only; a new finding that staff-promised restocks often don't arrive; a new "under-provisioning for real attendees" candidate loop, independently corroborated by two respondents; every respondent who offered a fix suggestion converged on the same one (extend closing time, don't shift the start time); across six respondents there is **no single population-level attendance trend** — some rising, some falling, one flat at zero all semester. This "no single trend" finding recurs as a headline caution against over-claiming throughout the later Process Trace / BOT Map documents.
- **Decision D-10 — cross-validation practice:** this reading of the five interviews was cross-checked against neha771's *independent* synthesis of the same transcripts (`student_interview_summary_neha.md`). Two corrections were adopted specifically *because* her independent reading was better supported: "monthly" (not "weekly") cycle terminology, and reconciling an apparent two-peak crowding contradiction into a single continuously-building crowd cresting near the 9:30 close.
- A further reconciliation pass (`1f074ae`) cross-checked two **independent transcriptions of the same five interviews** (one from `Interview data.pdf`, one from `Interview data.docx`) and confirmed they described the same five real respondents, not ten. It also caught a genuine ambiguity in the *source* data — a labeling collision where "Daily eater - 2" and "Daily eater - 3" both appear as labels but only one transcript follows both, meaning a true "-2" respondent's answers might be missing from the source entirely. **Decision D-11: this was flagged explicitly in both files rather than silently resolved by guessing**, since neither guess (mislabeled vs. genuinely missing) was verifiable from the source alone. This is a strong instance of the project's "don't invent certainty" discipline being applied to a data-hygiene problem, not just a findings problem.

### Session: 2026-09-06 (night) — sricharanperi: Finalize Tasks 3–5 + Consolidated Report
**Commit:** `f3faa1c` "Finalize Tasks 3-5 and consolidated Phase 1 report; fix cross-document consistency gaps"
**By:** sricharanperi with Claude (`session_01UURzEtfrZ3EbRSeQgXD8kp`)

- Built the final submission-ready Task 3 (Power-Interest/Leverage Map + Power Analysis Brief), Task 4 (Process Trace), and Task 5 (System Timeline/BOT Map) documents, each as matched `.md` + `.docx` pairs in the same style as Tasks 1–2: exact `project_framework .pdf` wording as section headers, no hedging, no citations (continuing decision D-5/D-7).
- Task 3 built from Prathyusha's governance research (Student Parliament roles, Mess Committee/Office split, four historical precedents) plus the already-audited power/leverage/authority analysis.
- Task 4 built from the six real interviews: monthly (not weekly) registration, disconfirmed peer-influence hypothesis, broken restock promises, zero real Skip Meal usage.
- Task 5: reconciled crowd-peak curve, confirmed R1/B1 feedback loops, plus the new under-provisioning loop.
- **Decision D-12:** Tasks 3–5 each end with an explicit "Outstanding Data Collection" section stating that mess-committee/vendor-side data will be collected and submitted later, rather than treating open items as answered — the same discipline as D-8/D-9, now made a standard closing section across every deliverable.
- **Consistency-fix pass across Tasks 1–2:** so nothing in the earlier deliverables contradicted the newer governance research — softened the unverified "faculty-chaired" Mess Committee detail, added the Student Parliament stakeholder to the Stakeholder Map (which Prathyusha's own research had flagged as missing), added the confirmed class-attendance-policy trade-off to Task 1's symptom table. This is a recurring maintenance pattern worth naming: **whenever a later deliverable's research contradicts or refines an earlier one, the earlier one gets patched, not left inconsistent.**
- New diagrams: monthly registration cycle, corrected daily attendance cycle, reconciled crowd-build curve, provisional governance timeline; some figures reused from `report/figures/` for Tasks 3 and 5.
- Built `Phase 1 Consolidated Report.pdf`: all five deliverables' main concepts combined into one **8-page** submission-ready PDF — explicitly **not** a full concatenation but a compressed synthesis — compiled via pandoc+tectonic to get a real, page-numbered table of contents. (Contrast with `Invictus_Phase1.pdf` later, which *is* a full concatenation of the five full deliverables — two different documents serving two different purposes: a short synthesis vs. the complete submission bundle.)

### Session: 2026-09-06 (late night) — sricharanperi: docx→PDF conversion pipeline, all five deliverables
**Commit:** `3885a3c` "Convert all five Phase 1 deliverables to submission-ready PDFs; fix docx-conversion bugs and strip tool metadata"
**By:** sricharanperi with Claude (`session_01UURzEtfrZ3EbRSeQgXD8kp`)

This is the session that established the **pandoc+tectonic conversion pipeline** documented in full in Section 5 below. Summary of what happened:

- Generated PDFs for all five individual deliverables directly from their `.docx` files (including a stakeholder-map infographic the user had manually added to the Stakeholder Map docx — preserved as-is, not regenerated), plus `Phase 1 - All Five Deliverables (Merged).pdf` (later renamed to `Invictus_Phase1.pdf` — see 2026-09-12 session) as a straight concatenation: 42 pages in, 42 pages out, no content loss, no re-summarization.
- **Bugs found and fixed during a full dry-run pass** (the direct ancestor of the same class of bugs re-encountered on 2026-09-12):
  1. A stray, unpopulated "Table of Contents" heading/link-block left over from each docx's own (unrefreshed) Word TOC field — stripped before re-typesetting.
  2. A dropped Unicode "interrupted relationship" glyph in the Stakeholder Map's diagram legend — replaced with a LaTeX-safe `X`.
  3. The user's manually-added stakeholder-map infographic had inherited a duplicate "Onion Proximity Map" caption from the image above it — relabeled to its own accurate caption, image itself untouched.
  4. The leverage-quadrant chart's title was clipped by Mermaid's own `quadrantChart` rendering at its original long title length — shortened to "Power vs Leverage" and regenerated.
  5. The provisional-timeline diagram was too tall for one page and got sliced across a page break — rebuilt as a horizontal (`LR`) Mermaid layout instead of vertical.
- **Decision D-13 (established here, still active): AI-pipeline residue removal.** Two "synthetic pilot/persona" methodology references in the Power Analysis Brief were removed — judged no longer load-bearing once six real respondents independently corroborated the same findings. And **every output PDF's `Title`/`Author`/`Creator`/`Producer`/`Subject` metadata is blanked** before submission (previously "LaTeX via pandoc" / "xdvipdfmx" was visible in PDF file properties). A final text-and-metadata scan across all seven PDFs confirmed zero remaining hits for any tool-fingerprint or AI-pipeline term. **This is the standing policy that governs every PDF this project submits — see Section 5's "AI-fingerprint scrubbing" subsection for the exact recipe.**

### Session: 2026-09-07 (post-session manual edits, uncommitted until 2026-09-12)

Between the previous commit (2026-09-06 23:41) and this timestamp (2026-09-06 23:39–23:51 — the same night, just after the AI session ended), the user made three changes **by hand, outside any Claude session**, which then sat uncommitted in the working tree for six days:
1. Renamed `Phase 1 - All Five Deliverables (Merged).pdf` → `Invictus_Phase1.pdf` (identical bytes, just the final submission filename).
2. Manually edited `Process Trace.docx` and `System Timeline BOT Map.docx` in Word: added a Table of Contents to each (via Word's TOC field feature), and removed a "Source"/"Respondent" identifier column (with interview dates like `2026-09-03`, `2026-09-06`) from the evidence tables in both — an apparent anonymization/cleanup pass.
3. Never regenerated the PDFs from these edits, and never committed anything.

This gap is exactly what the 2026-09-12 session below was asked to resume and close.

### Session: 2026-09-12 — Resume, close the gap, regenerate stale PDFs, fix TOC bug, write this file
**By:** sricharanperi with Claude (this session; no separate `Claude-Session` URL trailer on the resulting commit — see D-14)

**User's opening request:** "I want you to resume the session we did for the submission of the deliverables last week Sunday night. I am not able to retrieve that chat context." No other input or data was given beyond that sentence — everything else in this session was derived by investigating the actual state of the repo and filesystem.

**How I (Claude) approached it — reasoning, step by step:**
1. No memory existed yet for this project directory (checked `MEMORY.md` — didn't exist), so there was no shortcut; I had to reconstruct state from the ground truth.
2. Explored the working directory (`ls`), found the `Project/` subfolder was the actual git repo (the parent `Systems Thinking` folder is not a repo).
3. Ran `git log --oneline` and `git status` in parallel with directory listings of `deliverables/`, `report/`, `tools/`. `git log` immediately gave a highly detailed history (see Section 4 above, reconstructed from these same messages). `git status` revealed the uncommitted state described in "Session: 2026-09-07" above: a delete+rename of the merged PDF, two modified `.docx` files, one untracked new PDF.
4. To figure out *what* had changed in the two modified `.docx` files (binary diff is useless), I extracted both the git-committed (`HEAD`) version and the working-tree version to plain text via `pandoc … -t plain` and ran `diff`. This is what surfaced: a newly-added (but, it turned out, stale/frozen) Table of Contents, and a dropped "Source"/date column from two evidence tables.
5. Confirmed the renamed PDF (`Invictus_Phase1.pdf`) was byte-identical to the deleted `Phase 1 - All Five Deliverables (Merged).pdf` (same file size) — a pure rename, not a content change.
6. Checked file modification timestamps precisely (`ls -la` with full timestamps): the two edited `.docx` files were modified at 23:51, **twelve minutes after** their corresponding PDFs were generated at 23:39 — proof the PDFs were now stale relative to the docx content, not merely different by chance.
7. **Decision point — asked the user rather than guessing** (via `AskUserQuestion`, two questions): (a) whether to regenerate the two stale PDFs, just report the diff, or leave them as-is; (b) whether the merged `Invictus_Phase1.pdf` should also be regenerated to reflect the docx edits, or left untouched. **User answers:** regenerate the two stale PDFs (recommended option); leave `Invictus_Phase1.pdf`'s content untouched (do not regenerate the merge). This scoped the rest of the session precisely: only `Process Trace.pdf` and `System Timeline BOT Map.pdf` needed new work.

**Technical pass 1 — naive regeneration, and why it wasn't good enough:**
8. Checked what conversion tools existed on the machine: `pandoc` and `tectonic` were both present (`which pandoc tectonic`); no script or Makefile existed anywhere in the repo encoding the exact pipeline used on 2026-09-06 — that session's exact commands were never saved anywhere, only their *effects* (the committed PDFs) and a prose description in the commit message.
9. First attempt: `pandoc "Process Trace.docx" -o out.pdf --pdf-engine=tectonic` (the simplest possible invocation matching the commit message's "LaTeX-typeset via pandoc+tectonic, directly from the current .docx files"). This produced a PDF, but:
   - **Mistake/bug found #1:** it was **11 pages**, versus the previously-committed version's **8 pages**, for what should have been very similar content (same doc, minus one table column, plus a TOC block). This size mismatch was the first sign something about the pipeline wasn't being reproduced faithfully.
   - I rendered pages to PNG (`pdftoppm`) and visually inspected them (via the `Read` tool's image support). **Mistake/bug found #2, the important one:** a flowchart diagram ("Daily Attendance-Decision Cycle") that fit cleanly on one page in the already-committed, official PDF was **overflowing off the bottom of the page** in my naive regeneration — the last node ("Billed by registration count...") was clipped, and the figure's caption and page number were missing entirely because the image had pushed them off the page.
   - I confirmed this wasn't a pre-existing/acceptable defect by regenerating the *old* (pre-edit) docx with the same naive command and comparing: same clipping bug appeared there too (12 pages vs. the real committed file's 8), proving my plain command was **not** equivalent to whatever pipeline actually produced the committed PDFs — there had to be image-fitting logic in the original session that was never written down.
10. **Root cause and fix, iteration 1:** LaTeX's default `\includegraphics` (from `graphicx`, which pandoc uses) doesn't constrain an image to fit within the remaining space on a page — it just inserts it at whatever size the source specifies. I wrote a LaTeX header snippet (`fit-images.tex`) redefining `\includegraphics` to route through the `adjustbox` package's `max width`/`max height`/`keepaspectratio` keys, passed via `--include-in-header` to pandoc.
    - **Mistake #3:** first version errored out — `Package xkeyval Error: 'max width' undefined in families 'Gin'` — because plain `\includegraphics` (the `graphicx` `Gin` key family) doesn't understand `adjustbox`'s keys unless you either load `adjustbox` with its `export` option, or call `\adjincludegraphics` directly. **Fix:** redefined `\includegraphics` to call `\adjincludegraphics` instead of `\includegraphicsold`.
    - **Mistake #4:** with that fixed, the build succeeded, but the image was still overflowing — no visible change. **Root cause:** the pandoc-generated `\includegraphics[width=…,height=…]{…}` call already carries an explicit, absolute `height` from the docx's own image sizing; my `max height` key was listed *before* that explicit height in the merged key list, and in LaTeX key-value processing, keys are applied left-to-right, so the later explicit `height=` silently overrode my earlier `max height=` constraint. **Fix:** reordered the key list so my constraints (`max width`, `max height`, `keepaspectratio`) come *after* the pass-through of the original keys (`#1`), so mine win.
11. Re-ran; confirmed visually (rendered every page, read every image) that the diagram now fit correctly on its page with caption and page number intact — matching the quality bar of the already-committed original.

**Technical pass 2 — the "fix everything, not just make it work" decision, and a second real bug:**
12. Having gotten the PDFs to a reasonable state, I rendered and visually inspected *every single page* of both regenerated PDFs (not just the one known problem page) — this is a general practice worth naming: **verify PDF/diagram output by actually rendering to images and looking, not by trusting exit codes or page counts alone.**
13. While doing that inspection, I noticed the newly-added Word Table of Contents in both documents showed **the literal digit "1" next to every single entry** — every section, regardless of its real page number. **Diagnosis:** Word's TOC is a "field" that caches page numbers at the moment it's last refreshed (via Word's "Update Field"); if you insert a TOC and save without ever triggering that refresh, every entry keeps whatever placeholder/initial value Word wrote, which in this case was uniformly "1." Pandoc doesn't understand Word TOC fields as fields at all — it just flattens the field's cached text into plain paragraphs, so this defect passes straight through into any PDF built from the docx as-is.
14. **Decision point — flagged to the user rather than silently "fixing" it by guessing real page numbers.** I could not know the user's intended page numbers without recomputing them myself, and doing so myself and calling it "the TOC" would have been fabricating certainty (violates the same discipline the project itself keeps applying to its own research findings — see D-8/D-9/D-11). Asked the user directly (`AskUserQuestion`): regenerate with a proper, pandoc-native, accurate, hyperlinked TOC, or leave the broken one as-is. **User chose: regenerate with a proper TOC.**

**Technical pass 3 — building the native-TOC pipeline, and a third real bug:**
15. To get pandoc to generate its own accurate TOC, I needed to (a) strip the stale, hand-typed TOC block out of the source content, and (b) pass `--toc` on a *content* pandoc run rather than a direct docx→PDF run (pandoc's native `--toc` flag works the same way regardless of input format, but I needed to edit the content first, which meant going through an editable intermediate).
16. Converted each `.docx` to **standalone Markdown** (`pandoc … -t markdown --standalone`), which captures the docx's core-properties (`title`, `subtitle`) as YAML front matter — needed because the title page text ("Current-State Process Map / Process Trace" / "IIIT Hyderabad Breakfast Mess System") turned out to live in docx *metadata*, not in the document body text, and would otherwise have been silently lost by going through a markdown round-trip.
17. Wrote a small Python script to programmatically strip the stale TOC block: find the line starting `# Table of Contents`, delete through (but not including) the next top-level `# ` heading. Ran it on both documents.
18. Re-ran pandoc from the cleaned Markdown with `--toc -V toc-title="Table of Contents"` plus the same image-fitting header from pass 1.
    - **Mistake #5:** images failed to resolve (`Could not fetch resource media/image1.png`). **Root cause:** `pandoc --extract-media=DIR` nests the extracted files one level deeper than expected — a docx-internal path of `media/image1.png` gets extracted to `DIR/media/image1.png`, so when the markdown itself *also* says `media/image1.png` relative to some resource path, you need `--resource-path=DIR/media` (not `DIR`) so that `DIR/media` + `media/image1.png` resolves to the real `DIR/media/media/image1.png`. **Fix:** corrected `--resource-path`.
    - **Mistake #6, the more serious one:** once images resolved and the build succeeded, the native TOC now had *correct, non-"1" page numbers* — but visually inspecting every page again, I found **Figure 2 ("Daily Attendance-Decision Cycle") had drifted from page 4 (where it belonged, right after the paragraph that introduces it) to page 7**, several sections later. **Root cause:** going through the Markdown intermediate makes pandoc emit LaTeX images inside standard floating `figure` environments, and LaTeX's float placement algorithm is free to defer a float to wherever it next finds room — which, combined with the `adjustbox`-constrained sizing, pushed this particular figure several pages down the document, disconnecting it from its surrounding narrative. This hadn't happened in pass 1 because that was a *direct* docx→PDF conversion (pandoc's docx reader apparently doesn't route images through the same floating mechanism, or does so in a way LaTeX resolves differently) — the round-trip through Markdown was the specific trigger. **Fix:** added `\usepackage{float}` + `\floatplacement{figure}{H}` to the shared header, forcing every figure to render exactly where it appears in the source ("here," not floated), at the cost of occasionally awkward-but-acceptable page breaks — which the existing `max height` constraint from pass 1 already keeps from overflowing a page.
19. Re-verified: rendered and read every page of both final PDFs again. Confirmed all figures now sit in their correct in-sequence position with working captions and page numbers, confirmed the TOC page numbers are now accurate, and did a final content sanity pass (sorted, page-number-stripped text diff between this version and the pass-1 version) to make sure no prose content had been lost or duplicated in the process — differences found were only line-wrap reflow artifacts from different table column widths, not real content changes.

**Finishing — metadata, fingerprint scan, placement, git:**
20. Stripped tool-fingerprint PDF metadata using `pypdf` (`PdfReader`/`PdfWriter`, `writer.add_metadata({...})`), keeping the document's real `Title` but blanking `Author`, `Subject`, `Creator`, `Producer` — replicating the exact policy established in D-13, since no `exiftool`/`qpdf`/`pikepdf` were available on this machine (checked; only `pypdf` was installed).
21. Ran the same tool-fingerprint text scan as the 2026-09-06 session (`pdftotext … | grep -iE "pandoc|tectonic|xdvipdfmx|xetex|claude|anthropic|chatgpt|ai-generated"`) — clean, zero hits, on both files.
22. Copied the two final PDFs over the stale ones in `deliverables/phase-1/`.
23. **Decision point on git — asked before touching anything:** confirmed with the user (two more `AskUserQuestion` calls) whether to commit at all, and whether to push. **User's answers, verbatim intent:** commit, but **authored as themselves, explicitly not attributed to Claude** ("chat with sricharanperi (me) as the commitor, not under the name of claude"); and yes, push to `origin/main`.
24. **Decision D-14 — this is a deliberate, explicit override of my own default behavior.** My harness's standing instruction is to append a `Co-Authored-By: Claude …` / `Claude-Session: …` trailer to every commit I make. The user directly instructed otherwise for this commit, and per that instruction's own stated precedence rule ("the user's own instructions about these lines... take precedence over this reminder"), I honored the user's instruction: the commit `0b83c06` carries no Claude attribution at all, authored purely as `sricharanperi <sricharanperi27@gmail.com>` (which was already the configured git identity in this repo — verified via `git config user.name`/`user.email` before committing, not assumed). **This is consistent with the broader AI-fingerprint-scrubbing policy (D-13) — the user does not want this repo's visible history/authorship to read as AI-assisted**, which is important context for any future session: **do not add Claude attribution to commits in this repo unless the user asks for it in that specific instance.**
25. Committed (`0b83c06`, message: "Rename merged Phase 1 PDF for submission and fix TOC in two deliverables") and pushed. `git push` succeeded cleanly: `3885a3c..0b83c06  main -> main`.

**Then — this file:**
26. The user asked for exactly the document you're reading now: an exhaustive, continuously-updated, single-source-of-truth master context file covering every decision, every pass, all research, mistakes, thought process, and enough detail that another agent/account could resume cold.
27. **Decision D-15, made by asking rather than assuming, given a real tension:** the user had *just* asked me to scrub AI-authorship from commits and PDFs (D-14, D-13) — a document that narrates "how the agent thought" and "how subagents were created" in loving detail is in direct tension with that, if it's tracked and pushed to the same GitHub repo. I surfaced this tension explicitly and asked two questions: (a) where should this file live — gitignored-but-in-repo, committed-and-pushed, or entirely outside the repo; (b) how should pre-2026-09-12 history (reconstructed from git log, not a lived transcript) be represented — flagged as reconstructed, or written identically to directly-observed sessions. **User's answers:** commit and push it (i.e., it's fine for *this specific file* to be AI-transparent even though the submitted deliverables aren't — the scrubbing policy applies to the graded artifact, not to this project-management file); and write reconstructed history with the same level of confidence/detail as directly-observed history, no hedging distinction. Both are reflected in how this file is written and in Section 0's maintenance instructions above.
28. Wrote this file (`MASTER_CONTEXT.md`) at the repo root. **Next action after writing:** commit and push it, same as any other significant change, per the instruction in Section 0.

**Agent/tooling notes for this session specifically:**
- No subagents (`Agent` tool) were spawned during this session. Every investigative and technical step above was done directly, in the main conversation thread. Rationale: none of the sub-tasks were independent enough to parallelize usefully (each pandoc/tectonic debugging step depended on the visual result of the previous one), and none produced large enough intermediate output to justify off-loading into a fork purely to save context. If a future session needs to, e.g., regenerate PDFs for *all five* deliverables in parallel, or run an independent second opinion on a finding, spawning parallel forks or fresh subagents would be the right call then — this session just didn't have that shape of work.
- Heavy use of direct visual verification via the `Read` tool's image support (rendering PDF pages to PNG with `pdftoppm`, then reading them as images) rather than trusting text-only signals like page counts or LaTeX warnings alone. This was the single most load-bearing technique in the whole session — every real bug found (the clipped diagram, the frozen TOC numbers, the drifted figure) was caught by looking at rendered pages, not by reading logs.
- `AskUserQuestion` was used four times this session, each at a genuine decision point where guessing would have meant either fabricating certainty (TOC page numbers) or making an irreversible/visible choice on the user's behalf (whether to touch the merged submission PDF, whether/how to commit and push, where this very file should live). This matches the project's own long-standing internal discipline (D-8, D-9, D-11) of not inventing certainty — applied here to the *process* of working on the repo, not just to its research content.

### Session: 2026-09-12 (Phase 2 kickoff, by teammate) + pull/review pass (by this session)
**Commit:** `b7c69ac` "task 6,7 with pending interview" — **By:** neha771 <nehasusan369@gmail.com>, pushed directly to `origin/main` at 14:45 that day (after this session's own `882cc72` MASTER_CONTEXT.md commit, so it was never seen locally until fetched). No commit body, no Claude-Session trailer — can't tell from the commit alone whether this was done with AI assistance or not.

**User's request this pass:** "Pull everything from the repo regarding the changes done by the other team members, understand what has been done, and update everything into the new thing and also into MASTER_CONTEXT.md."

**What I (Claude) did:**
1. `git fetch origin`, then compared `git log --oneline -1` (local) against `origin/main` — found one commit ahead (`b7c69ac`) not yet pulled.
2. `git log 882cc72..origin/main` to read the commit message before pulling anything, then `git pull --ff-only` (fast-forward only, deliberately — no merge commit needed since local had no divergent work).
3. `git show --stat b7c69ac` to see the full file list, then read both new markdown files in full and viewed the two newly-drawn diagram images (`diagram1-system-map.png`, `diagram2-underprovisioning-loop.png`) to actually understand the content, not just infer from filenames.
4. Confirmed via `git branch -a` there's only ever been a single `main` branch in this repo's history — no stray unmerged branches to also account for.

**What Phase 2 (Tasks 6-7) actually contains — understood from reading the files directly:**

**Task 6 — System Map (`06-system-map.md`).** Explicitly framed as a *synthesis*, not new research: "Everything below is built from facts already confirmed in Tasks 1-5. No new claim is introduced here." Section headers again follow the assignment framework's own wording (continuing D-7): Map Actors/Processes/Information/Resources/Constraints; Identify Conflicts and Dependencies; Represent Formal and Informal Structures; Highlight Bottlenecks, Tensions and Feedback Loops; Key Findings; Outstanding Data Collection. It lays the full system out as one flow diagram (`assets/diagram1-system-map.png`, newly drawn — actors, registration→billing→vendor forecast→service window→outcome, with three explicit bottleneck callouts and the Academic-administration disconnect shown as a red dashed line) plus the reused Phase 1 "Power and Information Flow Network" diagram. Names three concrete bottlenecks (the 4-day procurement lead time; the 9:30 AM close; zero formal channel between Academic administration and any mess-governance actor) and states the core synthesis finding plainly: every no-show exit path (cancel, Skip Meal, resale, waste) is a workaround for the same missing mechanism — nothing lets a same-day intent change affect billing or cooking.

**Task 7 — Systemic Problem Analysis (`07-systemic-problem-analysis.md`).** A classic events→patterns→structures→mental-models iceberg analysis, again headers matching the framework's wording: Distinguish Events, Patterns, Structures and Mental Models; Identify Reinforcing and Balancing Feedback Loops; Analyse Unintended Consequences; Identify Structural Causes and Systemic Tensions; Key Findings; Outstanding Data Collection.
- Explicitly labels the **mental-models layer as inference**, not fact — "none directly stated by name in any interview but consistent with everything observed" — the same "don't invent certainty" discipline as D-8/D-9/D-11, now applied to a new kind of claim (inferred beliefs, not just missing data).
- Names four loops: **R1 — Shifting the Burden via Mess Cell** (reinforcing; reuses Phase 1's `d10-loop-r1.png`), **B1 — Kitchen Waste Control via Skip Meal** (balancing but incomplete — flags that Skip Meal's zero real usage means it may not be closing the loop even on the kitchen's own terms, a sharper version of a Phase 1 finding; reuses `d11-loop-b1.png`), **B2 — Capacity Redistribution** (weak/broken — Kadamba doesn't organically redistribute to Bakul/Palash; capacity was instead added administratively by building Bakul), and a **candidate structure, not yet a confirmed loop** — under-provisioning for real attendees (new diagram, `diagram2-underprovisioning-loop.png`), explicitly split into an evidenced first half (inflated registration vs. low true attendance) and an unconfirmed second half (whether kitchen prep is actually calibrated to the low true-attendance figure rather than peak-hour demand).
- An unintended-consequences table covering five rules/actions, including a sharp new claim: the December 2024 cancellation-cap tightening (originally a Mess Office workload-reduction move, per Phase 1) "plausibly increases, not decreases, the pattern of uncancelled no-shows it targeted" — a fix that may be working against its own goal.
- Three structural root causes named directly: (1) billing decoupled from attendance — the single structure behind the largest share of symptoms; (2) the four-day procurement lead time as a fixed ceiling on system responsiveness, "not a policy choice any single actor is making badly"; (3) governance fragmented across four disconnected actors (Mess Committee, Mess Office, Warden, and functionally Academic administration), none of which holds both the authority and the incentive to fix the core mismatch end-to-end.
- Two systemic tensions named: individual rationality vs. system-level cost (everyone's individually-sensible choices sum to the paradox — "no single actor is behaving irrationally"), and administrative simplicity vs. demand-matching.

**Outstanding Data Collection (both Task 6 and Task 7):** both explicitly say the mess-committee/vendor interview is **pending institutional approval** — this is a firmer, more specific status than Phase 1's more general "will be collected and submitted later" language, and it's the same single pending interview referenced by both new documents (not two separate gaps). See Section 9.

**Decisions/patterns confirmed as continuing into Phase 2 (not new decisions, but worth recording that they held):**
- Phase 2 reuses D-7 (section headers match the assignment framework's own wording) and D-9/D-12 (explicit "Outstanding Data Collection" closing section) without any prompting from this session — evidence these are now settled team conventions, not just this-session choices.
- Phase 2 explicitly re-applies D-11's inference-vs-fact discipline to a new domain: labeling the entire "mental models" layer of an iceberg analysis as inferred rather than confirmed, and explicitly splitting the under-provisioning loop into its evidenced half vs. its unconfirmed half rather than presenting it as one settled loop.
- Diagram reuse pattern continues: three Phase 2 diagrams are literal copies of Phase 1 assets (`d10-loop-r1.png`, `d11-loop-b1.png`, `d14-power-flow-network.png`) rather than redrawn — same pattern already noted for `report/figures/` vs. `deliverables/phase-1/assets/` (Section 3's cross-reference notes, Section 9's open item about the two figure sets not being automatically linked). **This is now a three-way duplication** (Phase 1 assets / report figures / Phase 2 assets) worth watching if any of these diagrams ever need a correction.

**What I did *not* do this pass:** no docx/PDF conversion, no edits to the Phase 2 markdown content itself, no changes to Phase 1 files. This was purely a pull-and-document pass — the user's request was to understand and record what teammates had done, not to build on it yet. If the next ask is "now build the docx/PDF for Tasks 6-7," the playbook in Section 5 applies directly (Phase 2 docs currently have no Word-TOC or embedded-infographic complications like Phase 1 did, so the simple direct route in 5.1 is the likely starting point — check for a Word-authored TOC field first per 5.2's checklist regardless).

### Session: 2026-09-13 — Prathyusha's braindump #2: CFS/CDS interview, Kadamba kitchen visit, mess posters
**Commit:** `cb50d5c` "Add Prathyusha's 2nd braindump (CFS/CDS interview + Kadamba kitchen visit + mess posters)" — **By:** Prathyusha Kalluri, with Claude (this session's working directory was actually the *parent* `Systems Thinking/Project` folder, not the repo itself — see note below)

**User's request:** dictated a long braindump covering a CFS Chair interview and a Kadamba kitchen walkthrough, plus four WhatsApp-photographed mess posters already sitting in `deliverables/phase-2/assets/`. Asked for it to be saved (calling it the "2nd braindump," implying parity with the existing `prathyusha_braindump.md`) and pushed to `main` "with my github not as claude" — i.e. attributed to the user (Prathyusha), not to Claude.

**What I (Claude) did:**
1. The session's actual working directory was the parent `Systems Thinking/Project` folder (not a git repo itself — confirmed via the environment's own "Is a git repository: false" flag); found the real repo one level down at `Project/breakfast-paradox-systems-thinking/`, matching this file's own note in Section 1 about the parent folder holding unrelated course materials alongside the repo. **This session's actual repo path differs from the path recorded in Section 1** (`/Users/Shared/Files From e.localized/PDM/...`) — this session's environment reported the repo at `/Users/klprathyusha/Sem 3/Systems thinking/Project/breakfast-paradox-systems-thinking`, i.e. the same repo checked out to (or synced to) a different local path/machine account than whatever machine the path in Section 1 was recorded from. **Flagging this rather than silently editing Section 1's path**, since it's unclear whether this is a new checkout, a synced folder, or a renamed home directory — worth confirming with the user before treating either path as *the* canonical location.
2. Checked git state: no local `user.name`/`user.email` configured for this repo, no global config either. Checked `gh auth status` — logged in as `PrathyushaKalluri`. Fetched that account's real name and numeric ID via `gh api user`, then set this repo's local git identity to `Prathyusha Kalluri <87800998+PrathyushaKalluri@users.noreply.github.com>` (GitHub's own noreply-email convention: `<id>+<username>@users.noreply.github.com`) — chosen specifically so the commit attributes to her actual GitHub account (verifiable, clickable on GitHub) rather than either a bare unlinked name or a Claude identity. **This is a new git identity variant for the same person** — see the People table (Section 2) update above; her Phase 1 commits used a different, local-machine identity (`Kalluri Lakshmi Prathyusha <klprathyusha@Kalluris-MacBook-Air.local>`).
3. Read the existing `prathyusha_braindump.md` (braindump #1) in full to match its exact convention: raw idea clusters, each followed by a "**Mapping:**" note connecting it to specific existing deliverables/open items, ending in an "Open items" checklist. Also read `MASTER_CONTEXT.md` and `research/primary-research/index.md` to check what was already known vs. genuinely new (this is what surfaced the Bakul/Palash correction and the "mess staff/vendor walkthrough" open-item resolution below).
4. Read all four poster images directly (`Read` tool's image support) and transcribed each word-for-word rather than paraphrasing — the FSSAI Eat Right Campus certificate, the CDS Student Handbook, the CDS Food Card User Guide, and the main CDS overview poster (capacities/cuisines/kitchen-location table, meal-timing table, full three-tier meal-charge pricing table, QR/registration rules).
5. Wrote `prathyusha_braindump_2.md` at the repo root (sibling to braindump #1, matching its exact structural convention), covering: CFS/CDS org structure and the decision-escalation flow (student council → committee, feedback-intensity routing to CFS head vs. committee); the biweekly menu-rotation policy change (a concrete, already-exercised leverage-point precedent); the T-4-day registration/vendor cadence; quantified turnout numbers (breakfast 35-40%, general ~70%, high-demand items >90%) with the exact 9:20 AM arrival-spike / 9:30 AM cutoff / 10 AM refill-and-lights-off mechanism; Kadamba specifics (renovation not turnout-driven, 420 seats vs. 1200 turnout at a mandated 1:3 ratio); a self-identified new risk (menu transparency enabling informed non-attendance, partially undermining the anti-boredom rotation); the Bakul/Palash off-site-kitchen correction (§9 below); waste management and food-safety lab-testing process; and the full word-for-word poster transcriptions (§11, labeled `mess_poster_data` per the user's own naming).
6. **Correction surfaced during this pass, not just new data:** braindump #1 had recorded an open hypothesis that Bakul was "a converted warehouse, food partly cooked at Palash and shipped in." The new poster data + interview show this was **half-right, half-wrong** — both Bakul *and* Palash run off a shared off-site kitchen (not Palash cooking specifically for Bakul), a temporary arrangement until the **Felicity Kitchen & Dining Facility** opens (~December 2026). Documented as an explicit correction in braindump #2 §9, not a silent overwrite.
7. **Data conflict flagged, not silently resolved:** the CDS poster's table lists Kadamba's "Capacity" as 1200, while the interview separately states "420 seats, 1200 turnout, 1:3 ratio maintained." Likely the poster's "Capacity" column means registration/allocation capacity, not physical seats — but this is a guess, not confirmed. Flagged as an open item in braindump #2 rather than asserting either number as settled.
8. Staged and committed the braindump file plus the four poster JPEGs (`git add` on named paths, not `-A`, per this project's own git-safety convention) with a descriptive commit message, no `Co-Authored-By: Claude`/`Claude-Session` trailer — continuing D-14's standing policy (this repo's commits don't carry Claude attribution unless asked for in that specific instance; here the user explicitly asked for the opposite — her own authorship, not Claude's). Pushed to `origin/main` (`77fe7e9..cb50d5c`).
9. **This pass (second half of the session):** user asked to also update `research/primary-research/index.md` and this file, per the repo's own stated research-discipline ("only material logged here... is treated as ground truth," Section 0 of that index) and this file's own maintenance instruction (Section 0 above: "update this file after every significant piece of work"). Added a new dated row to the primary-research index for this interview/walkthrough/poster set; checked off the "Mess staff / vendor process walkthrough" item as resolved (with a caveat: this was the **CFS Chair**, not the CDS Chair Giri specifically — still worth a direct Giri interview); updated the Bakul-ratio open item with the partial re-evidencing from this pass; updated the breakfast-observation open item to note this interview gives an aggregate mechanism/number but not a real time-series.
10. **Section 9 (Open Items, below) update:** the single biggest standing open item in this file — "mess-committee/vendor-side interview... pending institutional approval" — is **not fully resolved** by this pass (that item specifically names a *mess-committee* interview, and Giri as CDS Chair is the more direct analog to that gap; this was the CFS Chair, a level up). But it substantially **narrows** that gap: real answers now exist for how stock assessment/consumption patterns work, how registration ties to vendor communication, and the governance escalation pathway — see the updated wording in Section 9.

**Decisions this pass:**
- **Decision D-17:** when a repo's git identity needs setting and the user says "not as Claude," resolve it to the user's actual authenticated GitHub identity (via `gh auth status` + `gh api user`) rather than a guessed name/email — use GitHub's own `<id>+<username>@users.noreply.github.com` convention when no public email is set, so the commit is verifiably attributed and clickable on GitHub, not just a plausible-looking name.
- Continuing D-4/D-6 discipline: braindump #2 mirrors braindump #1's format exactly (raw content + "Mapping:" notes + Open Items checklist) rather than introducing a new structure, since it's explicitly a same-genre document (a second braindump by the same person).
- Continuing D-11's "flag ambiguity, don't silently resolve" discipline: applied to both the Bakul/Palash correction (§9 of the braindump) and the Kadamba capacity-vs-seats conflict (§6 of the braindump) — both written up as open questions with the evidence for each side, not asserted as settled facts.

**What I did *not* do this pass:** did not attempt to interview Giri (CDS Chair) directly — that requires the user, not Claude, to conduct; did not build docx/PDF for this braindump (it's explicitly a scratchpad per its own closing note, same as braindump #1 — not meant to be submission-shaped yet); did not yet fold any of this braindump's content into the actual numbered deliverables (`01-system-context-brief.md`, `06-system-map.md`, `07-systemic-problem-analysis.md`) — that reconciliation pass (matching D-6's "raw notes get consolidated into polished deliverables" pattern) is still open, flagged in the braindump's own Open Items list.

---

### Session: 2026-09-12 (Task 6 completion pass) — lecture-aligned System Map + a real Causal Loop Diagram set

**User's request:** analyze what's missing vs. what exists for Task 6 (the user quoted the assignment's own Task 6 method line and stressed the deliverable is a "Rich Picture / Causal or Systems Map"); identify what Neha had committed and what was merged; determine whether a complete, submission-ready Task 6 document could be produced right now; if not, name exactly what data is missing and who needs to supply it, using secondary research/reasonable assumptions wherever defensible instead of blocking; scope this pass to Task 6 only (Task 7 explicitly deferred); and — the sharpest constraint — **go through the actual course lectures and build the deliverable using the specific models taught there** (the user named "business, environmental, technical, and customer" mapping and the iceberg/causal-loop models explicitly, stating grading is tied to lecture-taught frameworks).

**How I approached it:**
1. Re-confirmed the Phase 2 state already logged in the session above (Neha's `06-system-map.md` + `07-systemic-problem-analysis.md`, already pulled).
2. **Read the actual course lecture PDFs** (`Lecture 1,2,3.pdf`, `Lecture_4.pdf`, `Lecture_5.pdf`, `Lecture_6.pdf`, `Lecture_7 & 8.pdf`, `Lecture_9.pdf`, `Lecture_10.pdf` — all in the repo root) via `pdftotext -layout`, then grepped across all of them for the taught vocabulary (rich picture, iceberg, causal loop, reinforcing/balancing, CATWOE, business/environment/technical/customer) before reading the hits in full context. This is the first time in this project's history any session went to the primary lecture materials themselves rather than relying on `project_framework .pdf`'s summary wording — worth doing whenever a deliverable's *scoring* (not just its content) is in question.
3. **Confirmed there is no formal Checkland-style "Rich Picture" technique (cartoon icons, crossed-swords conflict glyphs, etc.) taught anywhere in these lectures** — zero hits for "rich picture" or "CATWOE" outside the one mention in `project_framework .pdf`'s own Task 6 row, where it's offered as an *alternative* label ("Rich Picture / Causal or Systems Map") rather than a required distinct technique. This matters: it means a clean flowchart-style System Map (what Neha already built) is the right form, not a hand-drawn cartoon — no need to redraw anything in a different visual style on that account.
4. **Found the exact framework the user was recalling**, in `Lecture_6.pdf`, "Activity 1: System Mapping Sprint": *"Each group draws: 1. Users 2. Technology components 3. Business factors 4. Social/environmental factors 5. Arrows showing relationships."* This is the literal source of the user's "business, environmental, technical, and customer" phrasing (Users ≈ "customer," Technology components ≈ "technical"). `Lecture_7 & 8.pdf`'s "System Mapping Example" (a Food Delivery App case study) demonstrates this same framework worked through with reinforcing/balancing loops and an explicit "what data flows exist" prompt answered as labeled arrows between named components (e.g., "Customer → Central Backend: transmits order data, cart contents, payment status").
5. Also pulled forward, from `Lecture 1,2,3.pdf`: the "Elements of a System" vocabulary (Components, Relationships, Boundary, Environment, Feedback Loops, Delays) and the "structure drives behaviour, not the push" framing; and from `Lecture_6.pdf`/`Lecture_9.pdf`: the formal Causal Loop Diagram definition (Senge's Fifth Discipline Fieldbook is cited as the lecture's own source) — reinforcing loops have an even number of `−` polarity links, balancing loops an odd number, and "when arrows form a circle, we get feedback loops" (i.e., a CLD must actually **close**, not just chain forward).
6. **Loaded this repo's own `systems-design-toolkit` skill** (`.claude/skills/systems-design-toolkit/SKILL.md`), which turned out to already encode this exact mapping — it lists deliverable #6 (System Map/Rich Picture) and **deliverable #7 (Causal Loop/Feedback Analysis — CLD)** as two *separate* items, gives the same `-->|+|`/`-->|-|` Mermaid polarity convention, and confirms `project_framework .pdf`'s Phase 2 deliverable list (`System Map`, `Causal Loop/Feedback Analysis`, `Systemic Problem Analysis`, `Reframed Systemic Problem Statement`, `Design Opportunity Statement` — five items from three activities) is the authoritative rubric, not a 1:1 activity-to-deliverable mapping like Phase 1 had.
7. **Gap analysis, stated plainly:** Neha's `06-system-map.md` fully satisfied `project_framework .pdf`'s literal Task 6 wording (its four section headers match verbatim) but had **no explicit Users/Technology/Business/Social-Environmental categorization** (the exact Lecture 6 rubric) and **no actual Causal Loop Diagram** — the "feedback loops" content that existed (in `07-systemic-problem-analysis.md`, reusing Phase 1's `d10-loop-r1.png`/`d11-loop-b1.png`) turned out, on inspecting the underlying `report/figures/d10-loop-r1.mmd` source directly, to have a **likely polarity error** on top of being the wrong deliverable's problem to fix (belongs to Task 7, deferred). Concretely, item #2 of the Phase 2 deliverable list — Causal Loop/Feedback Analysis — did not yet exist anywhere as a properly closed, polarity-correct diagram.
8. **Checked whether any of this was blocked by missing data — it mostly wasn't.** A System Map operates at a higher level of abstraction than the Process Trace, and Phase 1's existing evidence base (six interviews, portal screenshots, the full governance/power research, four historical precedents) was sufficient to build every part of Task 6 properly. The one already-known, already-logged gap (mess-committee/vendor interview, pending institutional approval) affects only the deepest kitchen-side mechanistic detail and was already flagged, not new.
9. **One additional gap surfaced by going back to `ProblemStatements.pdf`** (the original fictional puzzle brief) directly: the puzzle's own framing describes a second reinforcing loop — skipped breakfast → an ~11:15 AM energy crash → compensatory canteen/delivery purchase → paying twice → low-nutrition sluggishness → more late nights → more skipping. I checked this against all six real interview transcripts (`grep` across `research/primary-research/interviews/`) and found **only the first link is actually confirmed** (late sleep/late nights is the dominant, repeatedly-confirmed skip driver) — nothing in any real interview confirms or denies the energy-crash/compensatory-purchase/sluggishness half; no interview question has ever asked what a respondent does *after* skipping. This is a genuine, honest gap worth naming rather than silently assuming the fictional brief's mechanism is true — consistent with this project's standing "don't invent certainty" discipline (D-8/D-9/D-11/D-16).

**What I built (no blocking data gap, so built in full):**
- **`deliverables/phase-2/assets/diagram3-mapping-sprint-quadrants.mmd/.png`** — a new diagram explicitly grouping the system into the four Lecture 6 categories (Users, Technology components, Business factors, Social/Environmental factors) with cross-category relationship arrows, color-coded per category.
- **Four new, properly-closed, polarity-labeled Causal Loop Diagrams** (the actual "Causal Loop/Feedback Analysis" content, deliverable #7 of the skill's list, built here as Task 6's own feedback-loops section since Task 6's methods explicitly include "highlight...feedback loops"):
  - `diagram4-cld-r1-resale.mmd/.png` — **R1, reinforcing, confirmed.** Low perceived cost of over-registering → registration volume vs. real intent → unused registrations → Mess Cell resale activity → value recovered → (closes back to) low perceived cost. Framed explicitly as a Senge "Shifting the Burden" structure in the accompanying prose.
  - `diagram5-cld-b1-skipmeal.mmd/.png` — **B1, balancing as designed, broken in practice.** A standard Goal-Gap-Action balancing structure (kitchen over-prep gap → Skip Meal usage → reduced prep → smaller gap), with the Gap→Action link explicitly marked broken (dashed red, annotated "0/6 confirmed used") since the corrective action never actually fires.
  - `diagram6-cld-b2-redistribution.mmd/.png` — **B2, balancing as designed, broken in practice.** Kadamba nearing capacity → scarcity signal → intended redistribution to Bakul/Palash → eases Kadamba's load, with the scarcity→redistribution link marked broken (confirmed finding: one respondent registers at Kadamba *because* it's scarce — demand concentration, not redistribution).
  - `diagram7-cld-candidate-skip-crash.mmd/.png` — **R2, candidate/hypothesized, NOT presented as confirmed.** The original puzzle's energy-crash/compensation loop, drawn with the one confirmed link (late nights → skipping) in solid green and every downstream link in dashed pink, explicitly labeled "candidate... only A to B is confirmed."
  - All five new diagrams rendered via `npx @mermaid-js/mermaid-cli` (`mmdc`) — confirmed working in this environment at v11.17.0; not previously verified available in this project (earlier sessions only ever *reused* pre-rendered `.png`s, never rendered a fresh Mermaid diagram from scratch).
- **Substantially expanded `deliverables/phase-2/06-system-map.md`**: added the Users/Technology/Business/Social-Environmental section (with the new diagram) right after the existing actors/process/resources/constraints section; replaced the old one-line "feedback loops" mention with a full Causal Loop Diagram subsection carrying all four loops above plus interpretive prose in the same terse, evidence-first house style as the rest of the document; added two new Key Findings (the "only R1 actually closes, both designed balancing loops break at the link requiring self-sacrifice" insight, and the "every technology component is student-facing, the system is only instrumented on one side" insight); added the R2 loop's specific, low-cost data ask to Outstanding Data Collection.
  - **Deliberately did not add any meta-commentary naming the lectures or the frameworks by name inside the deliverable itself** ("as taught in Lecture 6," etc.) — this project's standing style rule (D-5) is direct factual statements with no process narration or citations, and the grading alignment comes from the content actually matching the taught structure, not from citing it. (That sourcing reasoning belongs here, in this file, which is exempt per D-15 — not in the submitted document.)
- **Built `System Map.docx` and `System Map.pdf`** — the first Phase 2 docx/PDF to exist. Used the Section 5 playbook's direct route (5.1): the markdown has no Word-authored TOC field to strip (unlike Phase 1's docx files), so this went straight to `pandoc ... --toc --include-in-header=fit-images.tex`, no markdown-round-trip figure-drift risk to worry about. Verified page-by-page by rendering to PNG and reading every page (per the 5.5 discipline) — one new, real formatting bug found and fixed this way:
  - **Mistake #13 (new):** the "Bottlenecks:" and "Tensions:" bold labels in `06-system-map.md` were immediately followed by a bulleted list with no blank line in between; CommonMark/pandoc's markdown reader treated the whole thing as one paragraph via lazy continuation, so the PDF rendered the bullets as literal inline `-` characters instead of an actual bulleted list. **Root cause:** missing blank line between a bold "label:" line and the list that follows it — this is a general markdown-authoring trap, not specific to this pipeline. **Fix:** inserted the blank line in both spots; re-rendered and confirmed proper bullets. Worth checking for elsewhere in this repo's other markdown sources if they're ever rebuilt from scratch.
  - Metadata stripped and fingerprint-scanned clean per the standard D-13 recipe (kept `/Title` = "System Map", blanked the rest).

**What was flagged but deliberately NOT fixed this pass (out of scope, belongs to Task 7):** the reused `report/figures/d10-loop-r1.mmd`/`d11-loop-b1.mmd` diagrams (still embedded in `07-systemic-problem-analysis.md`) have a likely CLD polarity inconsistency on the "financial pain of no-show" → "pressure to fix billing" link (an increase in pain is wired as *decreasing* pressure to fix, which reads backwards) — noted here as an open item (Section 9) for whoever next touches Task 7, rather than silently patched, since Task 7 is a teammate's document and this session was explicitly scoped to Task 6 only.

**What's still genuinely missing, and who needs to supply it (stated once, plainly, per the user's explicit ask):**
1. **Nothing blocks Task 6 from being submission-ready as of this pass.** It is complete: markdown, four new diagrams, docx, and PDF all exist and were verified page-by-page.
2. **One optional, low-cost data ask**, not a blocker: whoever conducts the next student interview (or a quick follow-up with any of the existing six respondents) could ask what they actually do in the hours after skipping breakfast, and whether it changes their evening routine — this alone would either confirm or kill the R2 candidate loop. This is a single extra question on an existing guide, not a new interview round, and does not require institutional approval (unlike the mess-committee/vendor interview).
3. **The pre-existing mess-committee/vendor interview** (pending institutional approval, logged since Tasks 3-5) remains the only hard blocker on anything, and it does not block Task 6's completeness — it only limits how deep the vendor/kitchen side of the map can go, which is already stated honestly in Outstanding Data Collection.

**Not yet done (explicitly deferred, per the user's own instruction to do Task 6 first):** Task 7 (`07-systemic-problem-analysis.md`) has not been touched this pass — it still uses the older, unclosed-loop versions of R1/B1, and does not yet cross-reference the new, corrected CLDs built here for Task 6. When Task 7 is picked up next, it should be updated to point at (or reuse) this session's `diagram4`–`diagram7` CLDs rather than keep its own separate, less-rigorous copies — continuing this project's established "later work patches earlier work for consistency" pattern (first named in the 2026-09-06 Tasks 3-5 session).

---

### Session: 2026-09-12 (Task 6 visual redesign) — colorful custom diagrams, a new visual-design skill, and one new method (Service Blueprint)

**User's request:** for Task 6, (a) research and implement any additional methods/systems/processes a system map like this could reasonably include, drawing on secondary research or comparable projects, and (b) stop generating Mermaid diagrams — the user attached a reference image (a course lecture slide, "System Mapping Example," a Blinkit-app case study: a radial hub-and-spoke layout with colored zone blobs for Users/Technology/Business/Social-Environmental, flat icons, curved labeled arrows) and said every diagram in this project should look like that instead: colorful, icon-rich, high-clarity, "not like a simple flow using Mermaid charts." The user also asked explicitly for a reusable design/UI skill capturing this, acknowledging Claude has no native image-generation model and asking that a workaround be found and packaged as that skill.

**What I did, in order:**

1. **Confirmed there's no text-to-image model available in this environment** (no DALL-E/Midjourney/Stable-Diffusion-equivalent tool exposed to this session) — this had to be solved with a different technique, not assumed away.
2. **Found a working rasterization pipeline** by checking what's actually installed: `rsvg-convert`, `inkscape`, `resvg`, and any headless Chromium binary were all absent; `cairosvg` was pip-installable but failed at import because it needs a native `libcairo` this machine doesn't have (didn't `brew install` it without asking — a system-library install is outside the scope of "just make a diagram"). **`Google Chrome.app` was present**, and `google-chrome --headless --disable-gpu --screenshot=out.png --window-size=W,H --force-device-scale-factor=2 file://...` reliably rasterizes an HTML+SVG file to a high-resolution PNG. This became the standing render pipeline. (macOS prints several harmless `CVDisplayLinkCreateWithCGDisplay` / `task_policy_set` stderr lines on every invocation — noise, not failure; only the PNG's existence/size matters.)
3. **Built a small reusable Python SVG toolkit** (`svgkit.py`) rather than hand-writing raw SVG per diagram: a `chip()` function (rounded node with a colored circular icon badge + two lines of text), `straight_arrow()`/`curved_arrow()` (the latter supporting a polarity `sign` badge and a `dash` style for a "designed but not firing" link — exactly what a correct Causal Loop Diagram needs), `marker_defs()`, and `wrap_svg()`/`render()` for assembly and rasterization. Alongside it, a hand-drawn icon set (`icon-defs.svg`): simple flat SVG `<symbol>`s for person, people, gear, phone, QR code, chat bubble, coin, clock, warning triangle, briefcase, building, cooking pot, toggle switch, calendar-with-X, shield, bowl, and a broken-link glyph — all original geometric shapes, not traced from any icon library, to avoid any licensing question.
4. **Redesigned every diagram Task 6 uses**, replacing the earlier Mermaid versions built in the previous session's pass, all in a consistent palette (orange=Users/student-decisions, blue=Technology/Process, teal=Business/Governance, purple=Social-Environmental/Feedback, grey=External, red=bottleneck/broken, amber=tension, green-vs-pink=confirmed-vs-candidate):
   - **`diagram1-system-map.png`** — the main process-flow System Map, rebuilt with icons, a governance row, dashed cross-links, and small flag-style callouts for the two bottlenecks and the resale/Skip-Meal tension (a "rich picture" touch — annotated, not just a clean flowchart — without needing literal Checkland cartoon notation, which the lectures don't teach anyway; see the prior session's finding). Replaces the old hand-drawn `diagram1-system-map.svg`, now deleted as superseded.
   - **`diagram3-mapping-sprint-quadrants.png`** — rebuilt as a genuine radial hub-and-spoke (a central "IIIT-H Breakfast Mess System" node, four soft-colored zone ellipses around it), directly in the visual genre of the reference image, using this project's own real content rather than tracing the Blinkit example.
   - **`diagram4`–`diagram7` (the four CLDs: R1, B1, B2, R2)** — rebuilt as circular closed loops with icon nodes and visible polarity badges on every link, replacing the earlier plain-Mermaid versions from the prior session (same underlying loop logic, same confirmed/candidate/broken-link semantics — only the rendering changed).
5. **Added one genuinely new method, per the user's request to research and implement additions**: a **Service Blueprint** (`diagram8-service-blueprint.png`) — a well-established service-design technique (frontstage/backstage/support-process swimlanes across a timeline, separated by a "line of visibility") that hadn't been used anywhere in this project before, despite being explicitly in scope of this repo's own `systems-design-toolkit` skill's method list. Mapped the breakfast cycle across five time stages (monthly registration → T-4-day vendor forecast → the 7:30-9:30 service window → 9:30 close/reconciliation → ongoing feedback), with a physical-evidence lane on top. This surfaced a new, real finding not previously stated anywhere in the project: **the line of visibility is crossed only twice a month** (registration, and rarely rating) — every actual go/skip decision happens entirely within a lane the backstage side never sees in time to act on. Added to the document as Key Finding #6, and as a new section, "Trace the Service Delivery Timeline."
   - Other candidate additions were considered and **deliberately not added**, to avoid forcing a fit: a formal Checkland Rich Picture (no lecture support, per the prior session's finding — would contradict, not extend, that finding); relabeling B1 as a "Fixes That Fail" systems archetype (doesn't actually match — nothing was ever *applied* and made things worse; B1 is a mechanism that simply never engaged, which is what the document already says). Not every available framework belongs in every deliverable; a technique was only added where it produced a genuinely new, evidence-grounded finding.
6. **Two real bugs hit and fixed while building the CLDs** (both are exactly the kind of "verify by rendering" catches this project's playbook already calls for):
   - **Icon mis-centering:** an off-by-arithmetic error placed each `chip()`'s icon `<use>` element near the left edge of its circular badge instead of centered on it, making every icon render as a barely-visible sliver. Root cause: the `x`/`y` offset was computed from the wrong reference point (`left+10-9` instead of the badge's actual center, `left+icon_r+10`, minus half the icon's render size). Fixed by computing the badge center explicitly first.
   - **Missing label pass-through:** in the shared `loop_diagram()` helper (used for all four CLDs), the edge tuple's `label` field was unpacked but never actually passed into the `curved_arrow()` call — so every "intended, but not confirmed/observed" annotation on a broken link silently disappeared from the rendered output, with only its polarity sign showing. Caught by reading the rendered PNG closely (the label text simply wasn't there), root-caused by grepping the intermediate HTML for the expected string and finding zero matches, fixed by adding the missing `label=label` argument.
   - A third, related layout issue (not a "bug" exactly): two edges sharing an endpoint in a triangular loop computed near-identical label/sign positions (both anchored at the straight-line midpoint), so one silently occluded the other. Fixed generally, not just for that one case, by moving the anchor point to a parameter along the actual bezier curve biased toward the source node (`t≈0.32`) — edges sharing a node no longer collide because their "near-start" points sit near *different* nodes. Documented in the new skill as a standing thing to watch for in any future polygon/loop-style diagram.
7. **Packaged all of this as a new skill**, `.claude/skills/systems-visual-design/SKILL.md` (plus `svgkit.py` and `icon-defs.svg` alongside it), exactly as the user asked ("I will use that skill wherever necessary") — it documents the no-native-image-gen limitation and the Chrome-headless workaround up front, the shared component library and its functions, the four layout patterns used so far (category/radial map, process-flow map, CLD, service blueprint) with which diagram exemplifies each, the color palette table, and a short workflow checklist (reuse an existing generator, plan coordinates before coding, render-and-read before calling it done, save the `.py` source alongside its `.png` the same way `.mmd` sources sit alongside their renders elsewhere in this repo).
8. **Moved the four `.py` diagram-generator scripts into `deliverables/phase-2/assets/`** (alongside their `.png` outputs, mirroring the existing `.mmd`-next-to-`.png` convention), re-pointed them to import `svgkit` from the new skill directory, and **re-ran every one of them from that final location to confirm byte-identical output** before treating the skill packaging as done. Deleted the now-superseded Mermaid `.mmd` sources for diagram3 and diagram4-7 (fully replaced, never shipped/committed elsewhere, no provenance reason to keep them) and the old hand-drawn `diagram1-system-map.svg`.
9. **Rebuilt `System Map.docx`/`.pdf`** from the updated markdown (new Service Blueprint section, updated diagrams, Key Finding #6) via the same Section 5 playbook, verified all 12 pages by rendering to PNG and reading each one, stripped metadata, and confirmed a clean tool-fingerprint scan (added `mermaid` to the grep pattern for this scan, on the logic that a diagram claiming not to be Mermaid should not accidentally still say so anywhere in its text).

**What this means for future diagram work in this repo:** the `systems-visual-design` skill (Section "Layout patterns" table above lists the reusable shapes) should be the starting point for any new diagram from here on, in this project or a future one — not Mermaid, per the user's explicit standing instruction. New diagrams should extend `icon-defs.svg` with new symbols as needed rather than duplicating icon markup inline.

**What's still open from this pass:** Task 7 (`07-systemic-problem-analysis.md`) still embeds the old Mermaid-rendered, Phase-1-reused loop diagrams (`d10-loop-r1.png`, `d11-loop-b1.png`) and the still-unaddressed `diagram2-underprovisioning-loop.mmd` (Mermaid, a linear chain not a loop, so it doesn't need CLD polarity treatment, but it is still Mermaid-rendered and would need a `systems-visual-design` pass if/when Task 7 is picked up — see Section 9). This session did not touch Task 7 at all, per the user's original instruction to finish Task 6 first.

**Immediate follow-up in the same session — two concrete gaps the user pointed out after reviewing:**

1. **Figure 2 (the Users/Technology/Business/Social-Environmental map) had zero connections between the elements *inside* each category** — only the four bold hub-to-zone arrows existed; a Users node never linked to another Users node, etc. Fixed by adding real intra-zone edges, each grounded in an already-confirmed fact (no new relationships invented): Users — Students→Mess Office staff (registers/billed), Mess Office staff→Vendors/kitchen (4-day order), Warden→Vendors/kitchen (contracts), plus Warden↔Mess Office drawn dashed grey and explicitly labeled "relationship unconfirmed" (this project's own Task 3 finding is that the Mess Committee/Office/Warden relationship is *not* confirmed — so the diagram says so rather than drawing it as solid); Technology — the registration portal drawn as the internal hub of its own zone, since QR/rating/Skip-Meal/cancellation are all confirmed to be features of that one platform; Business — registration, capacity and vendor contracts all shown converging on the 4-day procurement lead time; Social/Environmental — cuisine preference and the "complaint ignored" institutional pattern both connected to the four physical kitchens (the Kadamba-specific precedents and demand data ground both links).
   - **Mistake #18 hit and fixed:** the first attempt put text labels on these new intra-zone edges, but several sat between two adjacent, closely-spaced chips with no room for a label box, so the label rendered on top of (and obscured) the neighboring node's text. **Fix:** dropped labels on short adjacent connections (the line itself is enough to show "these are related"); kept labels only on longer connections that cross open canvas space. A second, related slip on the same edit — routing the "cuisine preference → four kitchens" connector by moving its *endpoint* coordinate outward to dodge the two nodes in between, which instead sent the arrow flying past its actual target and off the edge of the canvas — was fixed by keeping the endpoint fixed at the real target and using `curved_arrow`'s `bend` parameter (which only moves the bezier control point, not the endpoint) to route around the intermediate nodes instead.
2. **Figure 4 (Power and Information Flow Network) was still the old Phase-1-style diagram**, reused as-is, never redrawn in this session's visual language. Rebuilt from scratch via `gen_pfn.py`, reading the exact node/edge list back out of the original `report/figures/d14-power-flow-network.mmd` source first (9 nodes, 16 relationships) so nothing was dropped or invented in translation. Categorized every edge by type — solid teal/blue for confirmed formal or material flows (policy authority, four-day forecast/order, contract authority, food/service), dashed purple for feedback channels, dashed orange for the informal economy (resale, unofficial tools), dashed red for the three specific "no formal channel to Academic administration" gaps plus the "no direct channel" between Vendors and Students — each with its own legend entry. Output replaces the old shared Phase-1 asset in this deliverable's own `assets/` folder (renamed `diagram9-power-flow-network.png`, this task's own numbering, since it's now a wholly redrawn asset, not a reused copy — `06-system-map.md`'s image reference updated to match).
   - **Mistake #19 hit and fixed:** the three "NO FORMAL CHANNEL" arcs initially shot off the top of the canvas entirely. **Root cause:** `curved_arrow`'s `bend` is a *fraction of the edge's own length*, and these three edges are nearly horizontal (small vertical, large horizontal separation) — a bend fraction that looks like a gentle arc on a short, steep edge translates into a huge absolute pixel displacement on a long, near-horizontal one, since the "bend direction" (perpendicular to the edge) is nearly vertical for a near-horizontal edge. **Fix:** used much smaller bend magnitudes (`-0.06` to `-0.15` rather than `-0.35`) for these specific long, near-flat edges. **Generalizable lesson, added to the skill:** for any near-horizontal or near-vertical long edge, pick `bend` by the desired *absolute* pixel arc height divided by the edge length, not by eyeballing the same fraction used for short edges.
3. Rebuilt `System Map.docx`/`.pdf` again from the updated markdown and diagrams, re-verified the affected pages by rendering to PNG and reading them, re-stripped metadata, re-scanned clean.

Both new mistakes (#18, #19) and the general lesson from #19 were added to the `systems-visual-design` skill and to Section 8 below, so the next diagram built with this toolkit doesn't rediscover them.

### Session: 2026-09-13 — Full Task 7 rebuild: evidence-disciplined Systemic Problem Analysis (`prathyusha_task7.md`)

User asked for a complete, standalone rebuild of Task 7 ("Identify Systemic Patterns & Root Causes"), separate
from Neha's existing `07-systemic-problem-analysis.md`, following an extremely detailed ~4000-word spec: full
Iceberg Model (events/patterns/structures/mental models, each individually sourced and confidence-tagged),
feedback-loop re-verification, delays, unintended consequences, structural root-cause testing (not mechanical
5-Whys), systemic tensions, structural power/consequence asymmetries, missing-feedback analysis, workarounds-
as-signals, one system archetype (fit-tested, others explicitly rejected), a red-teamed systemic problem
diagnosis, prioritized findings, and a full validation-gap backlog with exact non-leading follow-up questions.
The user's explicit instruction: use real evidence only, never convert one respondent into a population claim,
never treat the fictional Rohan/Adit brief as evidence about IIIT-H, and make the document read as human-authored
(continuing D-5).

**What was done:** re-read both primary interview transcripts in full (2026-09-03 self-account; 2026-09-06
five-student set) and confirmed their content against what was already known from earlier in this session,
rather than re-deriving from summaries. Built out a full evidence base of 17 sourced events, 6 patterns, 9
structures, and 3 mental models (only the ones with real evidentiary support — no invented mental models),
each tagged ✅/🟡/❓/⛔. Re-verified all six of Task 6's carried-forward loops (R1, B1, B2, R2, B3, R3) against
closure and evidence rather than rebuilding them, and explicitly rejected the existing Task 7 draft's
under-provisioning "loop" as a linear chain, not a closed loop (it lacks an evidenced closing link back to
registration behavior). Ran the full ROOT-CAUSE TEST diagnostic on four candidate structural causes; two
(billing/registration decoupling, and fragmented decision rights across menu/kitchen-execution/academic-
timetable/billing) passed as genuine structural root causes, two (the T-4 procurement lock, information
coarseness) were explicitly demoted to "intermediate mechanism" rather than inflated into root causes. Tested
seven system archetypes; retained one (Shifting the Burden, via the registration/resale loop) and explicitly
rejected the rest, including declining to promote a "Fixes That Fail" candidate (a Dec-2024 cancellation-cap
tightening) because it rests on a single secondary source (Neha's parallel draft) with no primary corroboration
in this evidence set.

**Files produced:**
- `deliverables/phase-2/prathyusha_task7.md` (568 lines) — the main, submission-ready file, following the
  spec's exact 20-section skeleton, with the fictional brief used nowhere as evidence.
- `deliverables/phase-2/assets/task7/01_iceberg_matrix.md` through `08_validation_gaps.md` (all 8 mandatory
  assets), plus three conditional assets judged warranted by real evidence: `09_system_archetypes.md`
  (Shifting the Burden has real support), `10_quantitative_analysis.md` (real numbers exist: turnout
  percentages by category, the ₹48 price point, the T-4 lock, the 5/month cap), and
  `11_mental_models_evidence.md` (three mental models have direct or near-direct evidentiary support).

**Key decisions:** see D-26 through D-29 below. **Deliberately not done:** no new CLD image was rendered via
the `systems-visual-design` skill this pass — the consolidated CLD's Mermaid source (mandatory per the user's
spec) was written in `02_causal_loop_diagram.md`, but per D-20 the *visual* rendering for actual submission
should go through the SVG toolkit, not Mermaid; this is flagged as an open item (Section 9) rather than
skipped silently. No docx/PDF was built for `prathyusha_task7.md` this pass either — same open item.

### Session: 2026-09-13 (same day, later) — Two data corrections + a full systems-boundary expansion of Tasks 6 and 7, via multi-agent perspective synthesis

Two things happened this pass. First, the user gave two direct corrections to the just-completed Task 7 P0
validation gaps: (1) the CFS Chair's awareness of the turnout gap (a further tentative ~45% figure, alongside
the earlier 35-40%) has **never reached a registration or billing policy discussion** — resolving what had
been the single highest-priority open question; (2) Skip Meal declarations **do** reach the kitchen and are
used for same-week prep-quantity forecasting, distinct from the still-rigid T-4 vendor-sourcing lock — a
two-stage pipeline, not one rigid freeze. Both corrections were threaded through every affected asset file
(`04_root_cause_network.md`, `08_validation_gaps.md`, `11_mental_models_evidence.md`, `02_causal_loop_diagram.md`)
and the main Task 7 file, not just noted once.

Second, and much larger: the user gave an extremely long, explicit instruction to stop treating "the mess
system" as bounded by registration/billing/academic-timetable alone, and to think in every direction a real
systems thinker would — agriculture/logistics/procurement, per-mess kitchen-technology differences (Kadamba
automated vs. Yuktahar manual/live vs. Palash/Bakul's temporary shared external sourcing), institutional
finance and vendor economics, waste management and its missing link to policy, the campus Canine Council,
physical infrastructure per mess, the academic timetable's *own* defensible logic (explicitly rejecting "just
move the class time" as a naive fix), faculty and early-shift-staff access as invisible demand streams,
student health (menu nutrient-vs-taste philosophy, a real HFMD outbreak framed carefully as a risk-amplifier
never a cause), and a wide set of social/cultural/mental-model factors (early-bird identity, thinness-
reinforcement, social anxiety around visible food choices, cultural/regional variation in whether breakfast
is even a norm). The user explicitly authorized confident, undirected assumption-making for this expansion —
"you can always assume stuff," no new interviews required — and asked for a literal multi-agent approach:
separate "perspective agents" for each stakeholder group, questioned and synthesized by one system-thinking
agent.

**What was done:** launched four parallel `fork` subagents (inheriting this session's full context), each
covering a cluster of the named perspectives — (A) supply chain, kitchen technology, institutional economics;
(B) governance, academic-timetable rationale, faculty/staff access; (C) waste, Canine Council, environment,
physical infrastructure; (D) student health, social/mental-model, and cultural systems — each producing a
dense, evidence-tagged (OBSERVED vs. ASSUMED) written analysis with candidate causal loops. All four returned
successfully (one needed a retry after a transient model-availability timeout). Read all four in full and
synthesized them into a substantially rebuilt `06-system-map.md` (new sections: three-kitchens-not-one supply
chain, waste/environment/Canine subsystem, academic-timetable-as-boundary-condition, social/cultural/health
dimensions) and a correspondingly rebuilt `prathyusha_task7.md`, both introducing a new 🔷 ASSUMED evidence
tag (explicitly authorized this session, always distinguished from confirmed claims) alongside the existing
tag sets each file already used.

**The single most important finding this pass produced, from Cluster A/B's independent convergence:** the
institution has already built, and already run, a **working attendance-based billing model** — during LPG
shortages and holidays/fests, registration became non-compulsory (walk-in QR, no billing penalty for
non-attendance), and the cancellation cap was raised 5→10/month. This is now loop **B4**, the headline finding
of this pass: it reframes the standing model's decoupled billing from "an unsolved problem" to "a proven fix
the institution deliberately doesn't run as the default," most plausibly trading away vendor procurement
lead-time certainty. Ten more loops/chains were added or substantially revised across both files (R4-R7, B5,
B6, and a canine-concentration open chain), bringing the total tracked to fourteen. A new diagram
(`assets/diagram14-cld-b4-shock-adaptive.png`) was generated for B4 via the existing `gen_clds.py` /
`loop_diagram()` helper (per D-20, SVG-toolkit, not Mermaid) and wired into both documents.

**Deliberately preserved, not overridden:** the report explicitly states that a meaningful share of "low
breakfast turnout" is legitimate cultural/religious/individual variation, not dysfunction (new tension T6,
new Finding F7) — the expansion pass was told to widen the map, not to manufacture a bigger villain.

**Deliberately not done this pass:** no docx/PDF rebuild for either `06-system-map.md` (now substantially
longer than its last-built PDF) or `prathyusha_task7.md`; no further diagrams beyond B4 (the "Extended System
Boundary" overview diagram discussed but not built, given the volume of markdown content already produced —
see Open Items).



### Session: 2026-09-13 — Merge Prathyusha's braindump #2; deepen Task 6 from a linear pipeline into a real feedback system

**User's request (verbatim intent):** pull the repo and see what changed; update `MASTER_CONTEXT.md` accordingly; understand how the changes impact Task 6 and implement all of it correctly; use "the braindump" to drive the changes; push everything as a full commit attributed to Prathyusha where relevant, and "for every commit, I hope you are doing it" (i.e. commit regularly, not hold everything in one uncommitted batch); review everything fully before calling it done. The user's core complaint: the System Map "is not thought through... doesn't have a lot of logical things... very shallow... needs to be very deep... the cycle looks very linear... there is not systems thinking." Also asked to "look at edge cases" and "all the different systems available at Triplett... how the systems interact." **"Triplett" could not be confidently resolved to any known term, person, or place in this project** (not a stakeholder, mess, department, or document name found anywhere in the repo) — treated as an unresolved dictation/transcription artifact rather than guessed at, and the substantive, actionable parts of the request (edge cases; multiple interacting sub-systems) were acted on directly without waiting to clarify it, consistent with this session's Auto Mode instruction to make a reasonable call and let the user redirect if needed. If "Triplett" resurfaces in a future session, flag it the same way rather than assuming a past guess was right.

**What I found on pulling:** `git fetch` showed `origin/main` one commit ahead (`63ba863`, Prathyusha Kalluri). That commit was itself a follow-up to an earlier one (`cb50d5c`, not yet in this session's local history either) adding `prathyusha_braindump_2.md` and four photographed CDS/CFS posters, then logging both in `research/primary-research/index.md` and in Prathyusha's own edit of `MASTER_CONTEXT.md`. **This is the first time in this project's history that a teammate has edited `MASTER_CONTEXT.md` independently** — confirmation the single-source-of-truth file is actually working as intended across the team, not just within this session.

**Merge mechanics:**
1. Committed this session's own already-in-progress, uncommitted work first (`2154c7b`, the Task 6 visual redesign + Figure 2/4 follow-up from the prior session entries above), so it couldn't be lost or tangled up in the incoming merge.
2. `git merge origin/main` — conflicted, as expected, since both sides had independently edited `MASTER_CONTEXT.md`. Four conflict regions: the Phase 2 repo-map entry (both sides had rewritten the same lines — merged by hand, keeping this session's fuller asset description plus Prathyusha's poster-file note), the Research Inventory intro paragraph (merged both framings into one), a **duplicate decision ID** (both sessions had independently used "D-17" for a different decision each — resolved by keeping this session's D-17 through D-21 as-is, since they were dated 2026-09-12, and renumbering Prathyusha's git-identity decision to D-22, dated 2026-09-13 — **decision IDs are ordered by the date of the decision, not by which commit reached `origin/main` first**), and the Open Items header/mess-committee-interview item (merged, keeping the fuller narrowing detail from Prathyusha's version plus this session's Task-6-resolved and Task-7-Mermaid notes from the other side). One further slip caught on inspection after the textual merge looked clean: a leftover duplicate paragraph (the old HEAD-side Research Inventory intro, not fully removed by the first conflict-block edit) had to be found and deleted separately — conflict markers can look fully resolved (no `<<<<<<<`/`=======`/`>>>>>>>` left) while still containing duplicated content from an incomplete edit; re-read the merged section after resolving, don't just check for marker absence.
3. Committed the merge (`4c7038d`) and pushed immediately, before starting any new work — so the merge itself was never at risk of being lost or re-tangled by subsequent edits.

**Reading the new primary source before touching anything:** read `prathyusha_braindump_2.md` in full (not just the diff summary), and opened the actual main CDS overview poster image directly to spot-check the transcription against the source photo (matched exactly: capacities, cuisines, kitchen types, meal timings, the full three-tier pricing table) — confirms the braindump's self-description as a word-for-word transcription is accurate, not paraphrased or approximated. This is the project's first real governance- and kitchen-side primary source: a CFS Chair interview (CDS's parent-department chair, not CDS Chair Giri directly), a Kadamba kitchen equipment/inventory walkthrough, and four posters (FSSAI certificate, CDS Student Handbook, CDS Food Card User Guide, main CDS overview poster).

**Diagnosis of why the System Map read as shallow and linear, stated plainly:** every prior version of Task 6 had a single "Vendors / Kitchen" box and a governance row of three named-but-thin actors (Mess Committee/Mess Office/Warden), because that was genuinely the limit of what was confirmed at the time — the kitchen and governance side had no real data behind it beyond the four-day lead time. The user's complaint was accurate given what existed; the fix wasn't better writing, it was that the underlying evidence to write from didn't exist until this new source landed. This is worth stating directly rather than treating the earlier version as simply a worse effort at the same task.

**What I built, using the `systems-visual-design` skill (Section 5.6) throughout — no Mermaid:**
1. **Two new causal loop diagrams**, extending `gen_clds.py`'s existing `loop_diagram()` helper:
   - **B3 — Menu Rotation Governance Loop.** The project's **first fully confirmed, already-exercised closed loop** — every prior loop (R1, B1, B2, R2) was either a candidate or broke at one link; this one is neither. Menu fatigue → feedback intensity → escalation via CDS Student Council → CDS Committee approval → menu variety → fatigue reduced, closing the loop. Every link sourced directly to the CFS Chair interview.
   - **R3 — Menu Transparency Backfire Loop.** The interviewee's own self-identified, unprompted second-order risk: the same rotation that fixed menu fatigue also makes the menu predictable, which may let students selectively skip on disliked-menu days — a candidate, reinforcing loop layered on top of B3's confirmed mechanism, with only its first link (rotation → predictability) confirmed and the rest marked candidate, matching the R2 loop's existing visual convention exactly (green solid = confirmed, pink dashed = not).
2. **A new "CDS/CFS Governance and Escalation" diagram** — the full org structure (CFS → CDS → CDS Chair/Committee/Student Council/per-mess admin, CFS Head handling low-intensity feedback directly, sibling out-of-scope departments CLS/CWS shown for boundary context), the intensity-based escalation split, a green "already fired once" precedent badge pointing at the menu-rotation policy change, and — the important part — **explicit, visually distinct (dashed grey, "unconfirmed") flags on three separate places where this new vocabulary might, or might not, name the same bodies Phase 1 already named differently**: CDS↔Mess Office, CDS Committee↔Mess Committee, CDS Student Council↔Student Parliament. A fourth overlap (Warden↔CFS itself, since CFS's own name is literally "Campus *Facilities* Services" and its infrastructure fee funds exactly what Phase 1 said Warden owns) was noticed independently during this session's own reasoning — not stated in the braindump — and flagged in text with the same discipline, not asserted as fact. **None of these four are merged or assumed identical anywhere in the document**, continuing D-11's "flag ambiguity, don't silently resolve it" discipline into new territory (reconciling two different research sessions' independent terminology, not just one session's internal data conflict).
   - **Mistake #20 hit and fixed while building this diagram:** a green "already fired once" precedent badge was positioned so its text rendered on top of, and overlapped, the "CDS Committee" node it was meant to sit beside. Fixed by moving it to its own clearly bounded box below-left of the node instead of floating text near it with no background.
3. **A new "Kitchen and Supply Subsystem" diagram** — Kadamba's kitchen redrawn as four interacting sub-systems around a central hub, not a single box: **supply chain** (open-tender outsourced catering, T-4 registration lock, weekly consumption-pattern-driven stock assessment, the exact turnout percentages by category); **equipment** (roti line, multi-item cooker with physically separated veg/non-veg, a curry/steaming machine specifically redesigned to tilt-drain after the older manual-lift method was flagged as unsafe, a knife sterilizer, prep automation, a mostly-unused butchering section since meat now arrives pre-butchered); **inventory** (cold + dry storage run by **one person** on FIFO/FEFO — flagged in red as a confirmed single point of failure); and **waste and food safety** (production/plate waste to an external compost facility; contamination-suspect samples to a lab, tied explicitly to the externally-audited FSSAI "Eat Right Campus" 5-star certification, shown as a green "externally validated" badge). A dashed side-note explicitly states Bakul/Palash are a *different*, off-site kitchen system, not this one — avoiding the reader conflating the two.
   - **Mistake #21 hit and fixed:** a stray, malformed circle element (a typo — `cy="1170+80"` written as a literal string instead of a computed value) rendered as an unexplained green blob floating off the top edge of the canvas. Found by looking at the rendered image, not by reading the generator code first — the same "render and read, don't just reason about the code" discipline this whole toolkit depends on.
4. **Real intra-category interconnections added to the existing Users/Technology/Business/Social-Environmental diagram** were already done in the prior "Figure 2/4 follow-up" session entry above — not repeated here, but this pass's governance/kitchen diagrams are a direct continuation of the same "don't leave a category as an unconnected list" principle applied at a deeper structural level.

**Rewriting `06-system-map.md`:** substantially expanded nearly every section — Actors (CFS/CDS org structure, Food Card/non-student diners, outsourced catering, edge cases: automatic vegetarian default, mess exemptions, medical delivery, Spot-dining throttling near session end), Resources (kitchen equipment, Infrastructure Fee), Constraints (three-tier pricing, 1:3 seat ratio, single-person inventory risk), two new tables (dining-hall capacity/cuisine/kitchen-type; three-tier meal pricing), the Bakul/Palash correction with the Felicity Kitchen transition explicitly marked as a system-in-transition not a static fact, the Kadamba-capacity data conflict (1200 vs. 420) flagged rather than resolved, the full governance section rewrite, two new CLDs, and substantially rewritten Key Findings (9 findings now, up from 6 — including a materially more precise finding that the system is not *uniformly* unresponsive to feedback, only unresponsive at mechanisms that ask individual students to bear a cost) and Outstanding Data Collection (Giri interview still needed; three-way terminology reconciliation; the Kadamba capacity ambiguity; the tender cost-passthrough question; the R3 interview question).
- **Mistake #22 (a repeat of Mistake #13's root cause, in new content):** a bold `**label:**` line immediately followed by a `- ` list with no blank line between them collapsed into one run-on paragraph with literal inline dashes, in the Outstanding Data Collection section's new "in priority order" list. Found by rendering and reading the final page, exactly the same way #13 was found. **This is now the second time this exact class of markdown authoring mistake has shipped a real defect in this project — worth treating as a standing thing to scan for (`grep` a document for a bold label line immediately followed by a `- ` line with no blank line) whenever writing a new bulleted list under a bold lead-in, not just relying on catching it by chance during page-by-page review.**

**Rebuilding the PDF surfaced one more real, previously-latent bug:** the ₹ (rupee, U+20B9) character — needed for the new pricing/fee tables — has no glyph in the default LaTeX Modern Roman font tectonic's pandoc template uses, so it silently rendered as a missing-character box with a compiler warning. **Fixed via `\usepackage{newunicodechar}` + `\newunicodechar{₹}{Rs.\,}`** in the shared `fit-images.tex` header — this substitutes the character only at LaTeX-compile time, so the markdown/docx source keeps the real ₹ symbol (nicer for anyone reading those directly) and only the PDF rendering path needed a fix. Added to the Section 5 playbook below since any future deliverable quoting rupee amounts will hit this identically.

**Verification:** rendered and read all 21 pages of the rebuilt PDF individually (per the standing 5.5 discipline) before calling this done — caught both the bullet-list bug and confirmed every new diagram fit its page correctly with no clipping or drift.

**Commits this session (separate, in order, each pushed promptly rather than batched):** `2154c7b` (prior session's pending visual-redesign work, committed first to protect it), `4c7038d` (the merge with Prathyusha's braindump-logging commit, conflicts resolved by hand), `ecaf2e3` (this session's Task 6 content deepening: new diagrams, rewritten markdown, rebuilt docx/PDF, the two font/list bugs).

**What was deliberately not done this pass:** Task 7 (`07-systemic-problem-analysis.md`) was not touched — it still has the older, thinner governance/kitchen framing and the same terminology (Mess Committee/Office/Warden) without the new reconciliation flags. It should get the same treatment Task 6 just got, in a future pass. Giri (CDS Chair) was not interviewed — that requires the user/team, not Claude. No attempt was made to resolve any of the four terminology-reconciliation questions independently (e.g. by inference or pattern-matching) — they are stated as open questions requiring a direct human answer, consistent with D-11.

**Immediate follow-up, same day — a real finding from re-reading the source images directly.** The user asked specifically to re-open all four poster photographs and extract everything, framed as "this is the truth of the mess displayed publicly... this shouldn't be happening," with an explicit instruction to change only the directly-affected part of the deliverable, not to do a broader pass. Opened all four poster images directly (not just re-reading the existing transcription) and found one genuine, narrow, citable anomaly: **the CDS Food Card User Guide poster contradicts itself** — it names the CDS Portal as `dining.iiit.ac.in` at the top and `mess.iiit.ac.in` at its own bottom contact line, on one physical sign. Checked the other three posters specifically for the same class of error: the main CDS overview poster states the same URL (`dining.iiit.ac.in`) consistently in both its own top and bottom sections — so this is not a project-wide naming ambiguity like the CDS/Mess Committee question, it is a **one-poster proofreading error on CDS's own signage**, physically on display at Kadamba. The original transcription in `prathyusha_braindump_2.md` §11.3 had glossed this exact line as "portal above" rather than transcribing it verbatim — which is precisely how the discrepancy went unnoticed the first time; corrected to the literal text and flagged explicitly, continuing that file's own "verbatim transcription, note discrepancies rather than silently reading past them" discipline. Per the user's explicit scope instruction, changed exactly two things and nothing else: (1) `06-system-map.md`'s **Resources** bullet, where the CDS Portal is first named, now states both URLs and points to Outstanding Data Collection; (2) one new bullet added to **Outstanding Data Collection** asking which domain is actually live. No other section of either file was touched. Rebuilt and re-verified `System Map.docx`/`.pdf` (still 21 pages, both affected pages re-rendered and read to confirm correct placement), re-scanned clean.

### Session: 2026-09-13 (same day, later still) — Final consolidation: one Task 7 file, a new PDF pipeline, both decks regenerated

User asked to (1) regenerate Task 6's docx/PDF and Task 7's docx/PDF, (2) consolidate Task 7 down to exactly
one MD/docx/PDF set — merging Neha's original `07-systemic-problem-analysis.md` with the interim
`prathyusha_task7.md` rebuild rather than keeping both — titled "Systemic Problem Analysis," not carrying a
personal name, (3) strip content from the polished PDF/docx that isn't either confirmed or a deliberate,
labelled assumption (i.e. drop the heavy research-audit scaffolding — evidence-coverage tables, validation-gap
Q&A, traceability registers, completeness audits — from the narrative, while keeping it available in
`assets/task7/`), and (4) fix the PDF layout itself: too much whitespace, wanted a non-LaTeX pipeline, correct
image sizing, and colorful icon-based diagrams (explicitly "Lucid"-style, explicitly not Mermaid, continuing
D-20) for every visual, including new ones the user asked for by name (an Iceberg Model diagram, a Root Cause
Network diagram).

**Task 7 consolidation:** read Neha's original `07-systemic-problem-analysis.md` in full again and pulled
forward every piece of content not already covered by the rebuild — specific food items reported running out
near the 9:30 close (idli, puri, bhatura, fruit), one sampled date's actual attendance split (Kadamba
51-79%, Bakul 11-31%), two named single-sourced incidents ("Let Them Eat Frogs," a November 2024 unilateral
non-veg cancellation at Kadamba, and the December 2024 cancellation-cap tightening), the standing caution
that no single population-level attendance trend exists across respondents, a single-sourced pattern about
the institution's general complaint-then-reactive-fix shape (cross-referencing an unrelated 2023 water-quality
episode), and a distinct systemic tension ("individual rationality against system-level cost") not previously
named as its own entry. Wrote the merged result to `07-systemic-problem-analysis.md`, replacing Neha's
original in place; deleted `prathyusha_task7.md` (D-34). The full research-grade rigor from the interim file
(the ✅/🟡/❓/🔷 emoji evidence-tag system, the 8 mandatory + 3 conditional `assets/task7/*.md` files) was kept
as backing research, not deleted — only stopped being duplicated into the polished narrative, which now uses
Task 6's existing prose-embedded confidence language (**confirmed** / **single-sourced** / **candidate** /
**assumed**) for one consistent voice across both final documents (D-35).

**New PDF pipeline (D-36):** replaced the pandoc+tectonic LaTeX PDF step with `pandoc` (markdown → HTML
fragment) + a custom stylesheet (`deliverables/phase-2/assets/report-style.css`) + Chrome headless
`--print-to-pdf`, wired together in `deliverables/phase-2/assets/build_pdf.py`. Docx generation is unchanged
(still plain `pandoc file.md -o file.docx --toc`). The new pipeline gives full CSS control over margins,
image `max-width` percentage, table density, and heading color that LaTeX's default float/spacing behavior
never gave cleanly (see Mistakes #6-9, #12 for the recurring pain this caused before) — confirmed working via
`pdftoppm`-rendered page images read directly, per the standing verification discipline. Both decks came out
substantially shorter and tighter than a LaTeX render would have: **System Map.pdf** at 25 pages (up in
content but not bloated, given how much the system-boundary expansion added) and **Systemic Problem
Analysis.pdf** at 11 pages (down sharply from what the un-condensed interim file would have produced).

**Three new diagrams**, all via the existing `systems-visual-design` skill (`svgkit.py`, no Mermaid, per
D-20), written in a new `deliverables/phase-2/assets/gen_task7_diagrams.py`: an Iceberg Model diagram
(`diagram15-iceberg-model.png`, the classic four-band Events/Patterns/Structures/Mental-Models visual this
project had never actually drawn despite using the framework in prose throughout Phase 2), a Root Cause
Network diagram (`diagram16-root-cause-network.png`), and a compact "loop dashboard" overview
(`diagram17-loop-overview-dashboard.png`, eight of the fourteen tracked loops as colored icon cards rather
than one unreadable mega-diagram). One real bug found and fixed during this pass: `chip()`'s second text line
is hardcoded to a dark grey fill, which is illegible against a dark chip background — hit when the first
Iceberg Model draft put white-on-navy mental-model chips on the bottom band; fixed by switching those chips to
a light fill instead of patching the shared `chip()` function (which other diagrams rely on with its current
behavior).

**Fixed while auditing this file:** a session-log header had gone missing somewhere across this
conversation's several `Edit` calls, leaving one real session's content orphaned under the previous entry
with no heading of its own — see Mistake #23. Two stale in-document cross-references to "Task 7, Section 19"
(a section-numbering scheme the old interim file used, now gone) were also updated to point at the new
consolidated file by name instead of by section number.

**Commits:** pending at time of writing this entry — see the commit immediately following this session log
update for the actual hash.

---

## 5. TECHNICAL PLAYBOOK — the docx→PDF pipeline (reusable recipe)

This is the exact, working recipe for turning a `deliverables/phase-1/*.docx` file into a submission-quality PDF, as of 2026-09-12. Read this before touching the pipeline again — it encodes three real bugs' worth of hard-won fixes.

**Tools available on this machine (verified 2026-09-12):** `pandoc`, `tectonic`, `pdftoppm`/`pdftotext`/`pdfinfo` (poppler-utils), Python 3 with `pypdf` installed. **Not available:** `exiftool`, `qpdf`, `pikepdf`, `PyPDF2`. Check again before assuming any of this if picking this up much later — the machine's tool set can change.

### 5.1 If the docx has no stale/broken Table of Contents
Simple, direct conversion:
```
pandoc "Document.docx" -o "Document.pdf" --pdf-engine=tectonic --include-in-header=fit-images.tex
```
This alone fixes image overflow (see 5.3) but does **not** add a table of contents — pandoc's docx reader flattens any Word TOC field into plain text as-is, whatever it currently says.

### 5.2 If the docx has a Word Table of Contents field (check first: does every entry show the same page number, e.g. all "1"? If so, it's stale/unrefreshed and must not be trusted or shipped as-is)
1. Extract to standalone Markdown, preserving title/subtitle metadata and extracting images to a known folder:
   ```
   pandoc "Document.docx" -t markdown --standalone --extract-media=OUTDIR -o doc.md
   ```
2. Strip the stale TOC block: find the line `# Table of Contents {#table-of-contents .TOC-Heading}` and delete everything from there up to (not including) the next top-level `# ` heading. (A short Python script does this reliably — see the session log above for the exact logic: scan for the first line starting with `# Table of Contents`, then scan forward for the next line matching `^# `, delete the span between.)
3. Convert the cleaned Markdown to PDF with a **native** TOC and the image/figure fixes below:
   ```
   pandoc doc.md -o "Document.pdf" --pdf-engine=tectonic --toc -V toc-title="Table of Contents" \
     --include-in-header=fit-images.tex --resource-path=OUTDIR/media
   ```
   Note the `--resource-path` points at `OUTDIR/media`, **not** `OUTDIR` — `--extract-media=OUTDIR` nests files one level deeper than you'd expect (`OUTDIR/media/media/imageN.png` for a docx-internal path of `media/imageN.png`), and the resource path needs to combine with the markdown's own `media/imageN.png` references to land on the real location.

### 5.3 `fit-images.tex` — the header-include that fixes image overflow and figure drift
```latex
\usepackage{float}
\floatplacement{figure}{H}
\usepackage{adjustbox}
\let\includegraphicsold\includegraphics
\renewcommand{\includegraphics}[2][]{\adjincludegraphics[#1, max width=\linewidth, max height=0.85\textheight, keepaspectratio]{#2}}
```
Two independent fixes bundled together, **both required** whenever converting through the Markdown-intermediate route (5.2); only the `adjustbox` half is needed for the direct route (5.1):
- **`float` + `\floatplacement{figure}{H}`:** forces every figure to render exactly in place ("here") instead of floating away to wherever LaTeX finds room — without this, a figure can drift several pages from its caption/surrounding text when converting via the Markdown intermediate.
- **`adjustbox` override of `\includegraphics`:** constrains every image to fit within the page's remaining width/height, preserving aspect ratio, so a tall diagram can never overflow off the bottom of a page. Two things about this specific override are easy to get wrong (both were gotten wrong once, on 2026-09-12):
  - You must route through `\adjincludegraphics`, not plain `\includegraphics` with `adjustbox`'s `export` option — plain `\includegraphics` doesn't understand `max width`/`max height` keys without the reroute.
  - **Key order matters.** The original pandoc-generated call already carries explicit `width=`/`height=` from the docx's own image sizing. Your `max width`/`max height` constraints must be listed **after** the pass-through of those original keys (`#1`), because LaTeX key-value parsing applies keys left-to-right and a later explicit `height=` silently overrides an earlier `max height=`.

### 5.4 Metadata stripping (AI-fingerprint scrubbing — standing policy since 2026-09-06, D-13)
Every PDF this project submits has its tool-fingerprint metadata blanked before being considered final, using `pypdf` since no other metadata tool was available:
```python
from pypdf import PdfReader, PdfWriter
reader = PdfReader(src)
writer = PdfWriter()
writer.append(reader)
title = reader.metadata.title if reader.metadata else None
writer.add_metadata({"/Title": title or "", "/Author": "", "/Subject": "", "/Creator": "", "/Producer": ""})
with open(dst, "wb") as f:
    writer.write(f)
```
**Keep `/Title`** (it's the document's real title, e.g. "Current-State Process Map / Process Trace" — not a tool fingerprint). **Blank `Author`, `Subject`, `Creator`, `Producer`** — these are the fields that otherwise leak `LaTeX via pandoc` / `xdvipdfmx (0.1)` etc.

After stripping, always run a final text+metadata scan for residue:
```
pdftotext "Document.pdf" - | grep -iE "pandoc|tectonic|xdvipdfmx|xetex|claude|anthropic|chatgpt|ai-generated|synthetic pilot"
```
Expect **zero** hits before considering a PDF submission-ready. (`synthetic pilot`/`persona` methodology language is included because the 2026-09-06 session specifically decided that language becomes residue once real data supersedes it — see D-13 — not because it's a tool fingerprint per se; use judgment on whether that specific phrase still applies to whatever document you're scrubbing.)

### 5.4b The ₹ (rupee) symbol needs a font fix in the PDF pipeline
The default LaTeX Modern Roman font tectonic/pandoc uses for PDF output has **no glyph for ₹ (U+20B9)** — it silently renders as a missing-character box with a compiler warning (`Missing character: There is no ₹ in font [lmroman10-regular]`), easy to miss unless you actually read the rendered page or grep the compiler's stderr. Fix in the shared header (`fit-images.tex` or equivalent), not in the source markdown/docx — this keeps the real ₹ symbol in the document source (nicer for direct readers) and only patches the rendering path:
```latex
\usepackage{newunicodechar}
\newunicodechar{₹}{Rs.\,}
```
Substitutes every ₹ with "Rs." at compile time. Any future deliverable quoting rupee amounts will hit this identically — check for it early (search the compiler output for "Missing character") rather than discovering it on a final page-by-page read.

### 5.5 Verification discipline (don't skip this)
Every conversion pass in this project's history that shipped a real defect (the 2026-09-06 session's five listed bugs; the 2026-09-12 session's three) was caught by **rendering pages to images and looking at them**, not by trusting pandoc/tectonic exit codes, page counts, or warning text alone:
```
pdftoppm -png -r 90 "Document.pdf" outprefix
```
then read the resulting PNGs. Check specifically: does every figure have its caption and page number visible (not clipped)? Is the figure positioned near the text that introduces it, not drifted pages away? Does the TOC (if any) show real, distinct page numbers, not a repeated placeholder? Do table columns fit inside the page margins without visibly bleeding past them?

### 5.6 Diagrams: use the `systems-visual-design` skill, not Mermaid
As of 2026-09-12 (Task 6 visual redesign), **do not generate Mermaid diagrams for this project** — the user explicitly rejected them as "clearly AI"-looking and asked for colorful, icon-based, hand-composed diagrams instead. Load `.claude/skills/systems-visual-design/SKILL.md` before building or editing any diagram. It documents: the reality that no text-to-image model is available here (this is hand-authored SVG rasterized via headless Chrome, not generative image AI); the shared `svgkit.py` component library (`chip()`, `straight_arrow()`, `curved_arrow()` with polarity/dash support, `render()`); the color palette; and four reusable layout patterns (radial category map, process-flow map, closed-loop CLD, service-blueprint swimlanes) each with a worked example already built for Task 6. Extend `icon-defs.svg` with new icons as needed rather than duplicating icon markup per script. **Known sharp edge (2026-09-13):** `chip()`'s second text line is hardcoded to a dark grey fill regardless of the `text_color` parameter — safe on a light chip, illegible on a dark one. Use a light `fill` for any chip needing two readable lines rather than patching the shared function, since other diagrams already depend on its current behavior.

### 5.7 PDF rendering: HTML + CSS + Chrome headless print-to-PDF, not LaTeX (as of 2026-09-13)
The pandoc+tectonic LaTeX route (5.1-5.4) remains valid and is still how `.docx` files are produced from markdown, but **PDF generation now goes through a separate, LaTeX-free pipeline**, built because the user found the LaTeX-rendered PDFs too whitespace-heavy with poor control over image sizing. Recipe: `python3 deliverables/phase-2/assets/build_pdf.py <source.md> <out.pdf>` — this strips the YAML frontmatter (title/subtitle) and renders it as a styled header, converts the remaining markdown to an HTML fragment via `pandoc -f gfm -t html5 --resource-path=<doc dir>`, wraps it with `deliverables/phase-2/assets/report-style.css` (Letter page size, ~0.6in margins via `@page`, `img{max-width:92%}`, colored headings matching the diagram palette, compact table styling), and renders the result with `Google Chrome.app --headless --disable-gpu --print-to-pdf=<out> --no-pdf-header-footer`. A `<base href="file://<doc dir>/">` tag in the HTML head means relative image paths (`assets/diagram1.png`) resolve correctly without needing `--embed-resources`. Verification is the same standing discipline as 5.5: `pdftoppm -png -r 90 out.pdf page` then read the PNGs directly — don't trust page count or file size alone. One known cosmetic side effect: `break-inside: avoid` on `figure`/`img` can leave a mostly-empty page when a tall image doesn't fit the remaining space and gets pushed to the next page whole; acceptable in practice (seen once across both 2026-09-13 rebuilds) but worth knowing if a page looks unexpectedly sparse.

---

## 6. RESEARCH INVENTORY

**Phase 2 (Tasks 6-7) introduced no new *primary* research sources at kickoff** (2026-09-12) — both documents drew exclusively on the primary research listed below plus the confirmed findings already written into Phase 1's deliverables (D-16). **This changed 2026-09-13:** Prathyusha's braindump #2 (CFS Chair interview, Kadamba kitchen walkthrough, 4 mess posters — see the 2026-09-13 session entries below) is genuinely new primary research. See those session entries for exactly which deliverables it has and hasn't been reconciled into.

**Course lecture PDFs (repo root: `Lecture 1,2,3.pdf`, `Lecture_4.pdf`, `Lecture_5.pdf`, `Lecture_6.pdf`, `Lecture_7 & 8.pdf`, `Lecture_9.pdf`, `Lecture_10.pdf`) were read directly for the first time in this project's history during the 2026-09-12 Task 6 completion pass**, to make sure Task 6 was built using the exact frameworks the course teaches (D-17), not just `project_framework .pdf`'s summary wording. Key extracted content, in case a future session needs it again without re-reading everything:
- `Lecture_6.pdf` — "Activity 1: System Mapping Sprint": map any system as Users / Technology components / Business factors / Social-environmental factors, plus arrows showing relationships. Also: the formal Reinforcing(R)/Balancing(B) loop definitions, sourced explicitly to Senge's *The Fifth Discipline Fieldbook* (already in this project's own bibliography).
- `Lecture_7 & 8.pdf` — "System Mapping Example" (Food Delivery App case study) demonstrating the same framework with reinforcing/balancing loops and a "what data flows exist" prompt answered as labeled arrows between named components; also the "Black Box" Input→Process→Output framing and the concept of Emergence.
- `Lecture 1,2,3.pdf` — "Elements of a System": Components, Relationships, Boundary, Environment, Feedback Loops, Delays; and the "structure drives behaviour, not the external push" framing (the Slinky/food-chain examples).
- `Lecture_4.pdf` / `Lecture_5.pdf` — the Iceberg Model (Events → Patterns → Structures → Mental Models), directly relevant to Task 7 when it's next picked up.
- `Lecture_9.pdf` — formal Causal Loop Diagram definition: "when arrows form a circle, we get feedback loops" — i.e. a CLD must actually close, not just chain forward.
- No lecture anywhere teaches a formal Checkland-style "Rich Picture" (cartoon icons, crossed-swords conflict glyphs) — confirmed by a zero-hit keyword search across all seven lecture PDFs. `project_framework .pdf`'s "Rich Picture / Causal or Systems Map" wording for Task 6 is offering alternative labels for one deliverable, not requiring a distinct hand-drawn technique.

**`ProblemStatements.pdf`** (repo root) — the four candidate puzzle briefs; "The Breakfast Paradox" is the one this team chose. Re-read directly during the Task 6 completion pass specifically to check whether its own fictional framing (the energy-crash/compensatory-purchase/pay-twice cycle) had ever been confirmed by real interview data — it has not, beyond its first link (late nights → skipping); see the R2 candidate loop in `06-system-map.md` and D-19.

**This repo's own `.claude/skills/systems-design-toolkit/SKILL.md`** — a project-specific skill encoding the full 15-deliverable framework, Meadows' 12 leverage points, the Power-Interest grid, and Mermaid CLD conventions. Load it (via the `Skill` tool) for any future Phase 2/3/4 work — it already had the exact System-Map-vs-CLD deliverable split this session had to otherwise re-derive from the lectures.

**Primary research (the project's actual evidence base):**
- `research/primary-research/interviews/2026-09-03_student-self-account-and-mess-system-overview.md` — the original respondent, a habitual skipper, general system informant (interviewed 2026-09-03).
- `research/primary-research/interviews/2026-09-06_five-student-interviews-guide1.md` — five more real students (2 daily eaters, 3 skippers), interviewed 2026-09-06 using Guide 1. This is the project's core quantitative-ish evidence base for Activities 4 and 5.
- `research/primary-research/interviews/student_interview_transcpirt_neha.md` and `student_interview_summary_neha.md` — neha771's independent transcription and synthesis of the same five interviews, used as a cross-validation source (see D-10, D-11).
- `research/primary-research/screenshots/` — portal screenshots of registration UI, meal capacities, rates, and edit-registration flows, used as ground truth for how the registration/billing mechanism actually works (not self-reported). Filenames are dated by the calendar date **shown inside** the screenshot's UI content, which can postdate the commit that added the file — don't assume the filename date is the capture date.
- `research/primary-research/team-contributions/2026-09-05_teammate-constraints-edge-cases-verticals.md` — a teammate's independently authored hypothesis/verticals document, kept verbatim (D-4).
- Two rounds of **synthetic (AI-simulated) pilot interviews** (`research/methodology/prathyusha_synthetic-pilot-interviews.md`) — explicitly non-ground-truth, used only to stress-test interview guide wording before running real interviews (D-2). Never cited as findings; explicitly flagged as weaker/removed once real data exists (D-13).
- `prathyusha_braindump_2.md` (2026-09-13) — CFS Chair interview (governance/decision flow, menu policy, registration/vendor cadence, stock assessment, waste/food-safety process) + Kadamba kitchen equipment/inventory walkthrough + word-for-word transcriptions of 4 CDS/CFS posters (§11: `mess_poster_data`). The project's first real governance/operations-side primary source. Single-sourced (one respondent, the CFS Chair, not yet the CDS Chair Giri) — see `research/primary-research/index.md`'s provenance caveat on this entry. **Reconciled into `06-system-map.md` (Task 6) on 2026-09-13** — see that day's session entry in Section 4. Not yet reconciled into Task 7, the Stakeholder Map, or the Power-Interest/Leverage Map (Section 9).

**Secondary research / methodology reading (folded into the interview standard and analysis approach, not cited directly in deliverables per D-5):**
- Erika Hall, *Just Enough Research*
- Nunnally & Farkas, *UX Research: Practical Techniques*
- Dumas & Loring, *Moderating Usability Tests*
- King, Churchill & Tan, *Designing with Data*
- Sterman, *Business Dynamics: Systems Thinking and Modeling* (added as a reference text 2026-09-05)
- Senge, *The Fifth Discipline* and *The Fifth Discipline Fieldbook* (`research/book-notes-fifth-discipline*.md`)
- Design Thinking Toolbox worksheets (`research/dt-worksheets/`) — the direct source of the Stakeholder Map's template structure (D-7)
- A larger set of HCI/UX textbooks (Buxton, Carroll, Becker, Constantine, Tidwell, etc.) present on disk as research input but **excluded from git** as copyrighted material not ours to redistribute (D-1) — see `.gitignore`.

**Reference frameworks:**
- `project_framework .pdf` — the assignment's own methods framework; every deliverable's section structure is written to match this document's own activity/method wording (D-7, recurring).
- `Courses-Syllabus_M26-V1.pdf` — course syllabus (lives in the parent folder, outside the git repo).

---

## 7. DECISION LOG (flat index, cross-referenced to Section 4 for full context)

| ID | Decision | Session |
|---|---|---|
| D-1 | Exclude copyrighted reference book PDFs from git via `.gitignore`, even though used as research input | 2026-09-03 |
| D-2 | Synthetic/AI-simulated interview data is explicitly marked non-ground-truth, never treated as a finding | 2026-09-05 |
| D-3 | Split Context Brief "Purpose" into true purpose / actual mechanism / gap between them | 2026-09-05 |
| D-4 | Preserve teammate research verbatim in its own file, not just paraphrased conclusions | 2026-09-05 |
| D-5 | Deliverable prose: no hedging, no citations, no process narration — direct factual statements only; drafts with narration archived, not deleted | 2026-09-05 |
| D-6 | Working/raw files get consolidated into one polished deliverable; redundant intermediates removed and cross-references fixed | 2026-09-05 |
| D-7 | Deliverable structure follows the assignment's own method-toolbox templates (`project_framework .pdf`, DT Toolbox), not ad hoc structure | 2026-09-05 |
| D-8 | Open research questions logged and left explicitly unanswered rather than guessed at | 2026-09-06 |
| D-9 | Split Activities 4/5 into "established now" vs. "completion plan for unobserved side"; standardized "Outstanding Data Collection" closing section | 2026-09-06 |
| D-10 | Cross-validate independent readings of the same raw data; adopt corrections where the other reading is better supported | 2026-09-06 |
| D-11 | Flag genuine source-data ambiguity explicitly rather than silently guessing a resolution | 2026-09-06 |
| D-12 | Standardize "Outstanding Data Collection" as a closing section across Tasks 3–5 | 2026-09-06 |
| D-13 | Standing AI-fingerprint-scrubbing policy: blank PDF tool metadata (keep Title), remove now-superseded synthetic-pilot language, scan final text for tool/AI residue — applies to every submitted PDF | 2026-09-06 |
| D-14 | This repo's commits do not carry Claude attribution unless the user asks for it in that specific instance — consistent with D-13's scrubbing policy | 2026-09-12 |
| D-15 | This file (`MASTER_CONTEXT.md`) is committed and pushed to the repo despite D-13/D-14, because it is a process/context document, not a submitted deliverable | 2026-09-12 |
| D-16 | Phase 2 (Tasks 6-7) built as a pure synthesis of Phase 1 findings, explicitly re-confirmed as introducing no new claims — same "don't invent certainty" discipline as D-8/D-9/D-11, now also applied to labeling an entire analysis layer (iceberg-model mental models) as inference rather than fact | 2026-09-12 (Phase 2 kickoff, by neha771) |
| D-17 | When a deliverable's grading is explicitly tied to course-taught frameworks, go to the primary lecture PDFs directly (not just `project_framework .pdf`'s summary wording) before building — this is how the Users/Technology/Business/Social-Environmental framework (Lecture 6) and the formal closed-loop CLD definition (Lecture 6/9) were correctly identified as what Task 6 needed | 2026-09-12 (Task 6 completion pass) |
| D-18 | Frameworks/lectures are applied through document structure and content, never cited by name inside the submitted deliverable itself — continuing D-5's "no process narration, no citations" rule; the sourcing reasoning lives only in this file (exempt per D-15) | 2026-09-12 (Task 6 completion pass) |
| D-19 | A candidate causal loop drawn from the original fictional puzzle brief (`ProblemStatements.pdf`) is included in the System Map only with its unconfirmed links visually and textually marked as such (dashed, labeled "candidate"), never presented at the same confidence level as a loop built from real respondent evidence | 2026-09-12 (Task 6 completion pass) |
| D-20 | All project diagrams are hand-composed SVG (via the `systems-visual-design` skill / `svgkit.py`), rasterized through headless Chrome — not Mermaid — per the user's explicit instruction that Mermaid output "looks clearly AI"; packaged as a reusable skill rather than a one-off fix | 2026-09-12 (Task 6 visual redesign) |
| D-21 | A Service Blueprint (frontstage/backstage/support-process swimlanes) was added as a genuinely new method for Task 6, chosen because it produced a real, previously-unstated finding (the line of visibility is crossed only twice a month); other candidate additions (a literal Checkland Rich Picture, relabeling B1 as "Fixes That Fail") were considered and deliberately not added because they didn't fit the evidence or contradicted an already-established finding | 2026-09-12 (Task 6 visual redesign) |
| D-22 | When a repo's git identity needs setting and the user wants a commit attributed to themselves "not as Claude," resolve it to their actual authenticated GitHub identity (`gh auth status` + `gh api user`), using GitHub's own `<id>+<username>@users.noreply.github.com` convention if no public email is set — not a guessed name/email | 2026-09-13 (renumbered from Prathyusha's D-17 on merge — two sessions independently used the same next-available ID; kept in chronological order by date, not by which commit landed on `main` first) |
| D-23 | When two sessions independently assign the same next-available decision ID during a `MASTER_CONTEXT.md` merge conflict, renumber by the *date the decision was actually made*, not by which commit reached `origin/main` first | 2026-09-13 |
| D-24 | New primary research that introduces a different organization's vocabulary for a body already named in an earlier deliverable (e.g. "CDS Committee" vs. "Mess Committee") is never silently merged or treated as confirming the same identity — every such overlap is flagged visually and in text as an open, unconfirmed question, extending D-11 from within-source ambiguity to across-source terminology ambiguity | 2026-09-13 |
| D-25 | New primary research that arrives as a raw braindump/scratchpad is reconciled into the relevant numbered deliverable(s) promptly, in the same session if practical, rather than left sitting as a scratchpad indefinitely — continuing D-6's "raw notes get consolidated" pattern, applied here across two different people's sessions rather than within one person's own drafts | 2026-09-13 |
| D-26 | Task 7 was rebuilt as a *second*, separate file (`prathyusha_task7.md`) rather than editing Neha's existing `07-systemic-problem-analysis.md` in place, because the two were authored independently against different levels of rigor — both are kept, matching Phase 1's established `prathyusha_0N-*.md`-alongside-numbered-deliverable pattern, rather than one overwriting the other | 2026-09-13 (Task 7 rebuild) |
| D-27 | A candidate causal chain/loop is only diagrammed as a loop if its closing link has direct evidence; the existing Task 7 draft's under-provisioning chain was tested and rejected as a loop (no evidenced link back to registration behavior) and used instead as a plain causal chain — extends D-19's "mark unconfirmed links as unconfirmed" into "don't call something a loop at all if it doesn't close" | 2026-09-13 (Task 7 rebuild) |
| D-28 | A structural cause is only labeled a root cause if removing it would plausibly stop the pattern; two evidenced structures (the T-4 procurement lock, aggregate-only feedback) were deliberately demoted to "intermediate mechanism" rather than counted as root causes, because each is downstream of / compounds a more upstream structural cause rather than independently sufficient | 2026-09-13 (Task 7 rebuild) |
| D-29 | A single secondary source (another team member's independent draft) is never promoted to a confirmed finding on its own — the existing Task 7 draft's "Fixes That Fail" candidate (a reported Dec-2024 cancellation-cap tightening) was flagged as UNKNOWN-REQUIRES VALIDATION rather than written up as a confirmed archetype, because no primary interview or institutional statement in this evidence set independently corroborates it | 2026-09-13 (Task 7 rebuild) |
| D-30 | When the user explicitly authorizes wide, undirected assumption-making to widen a system's boundary (rather than asking for new interviews), a distinct evidence tag (🔷 ASSUMED) is introduced rather than silently blending assumed content into the existing confirmed/candidate tag sets — every existing evidence-discipline decision (D-8, D-9, D-11, D-19) still applies: assumed content is used freely but never presented with the weight of a confirmed claim | 2026-09-13 (systems-boundary expansion) |
| D-31 | A large, multi-perspective ideation task the user explicitly frames as needing several "different perspective agents" is delegated to parallel `fork` subagents (not fresh general-purpose agents) specifically because each needs the full evidence base already established in-session to stay consistent with it — grouped into a small number of thematic clusters (here: four) rather than one agent per named stakeholder, to keep synthesis tractable | 2026-09-13 (systems-boundary expansion) |
| D-32 | A genuinely new, real historical mechanism the user reveals mid-project (here: the LPG-shortage/holiday non-compulsory-registration precedent) that directly contradicts an earlier finding's framing ("the institution has never solved this") is treated as grounds to *revise* that finding's framing, not merely append a footnote — Task 7's root cause section was rewritten to reflect "a proven fix, deliberately not the default" rather than leaving the older "unowned gap" framing standing unchanged alongside the new fact | 2026-09-13 (systems-boundary expansion) |
| D-33 | When new evidence reframes what portion of a documented "problem" is actually legitimate variation rather than dysfunction (here: cultural/religious/individual breakfast-skipping), that reframing is written into the deliverable explicitly as a finding and a tension, not treated as scope creep to exclude — an honest systemic diagnosis states the ceiling on how much of a gap should be treated as fixable, not just why the gap exists | 2026-09-13 (systems-boundary expansion) |
| D-34 | Once two independently authored versions of the same numbered deliverable both exist (Neha's original Task 7 draft and the interim `prathyusha_task7.md` rebuild), and the user asks for one consolidated file, the correct move is to merge into the deliverable's canonical numbered filename (`07-systemic-problem-analysis.md`) and delete the redundant interim file — not keep both indefinitely — reverting the D-26 "keep both" stance once the user explicitly asks for one | 2026-09-13 (final consolidation) |
| D-35 | A polished, submission-facing document shows only confirmed findings and explicitly labelled assumptions, in one consistent prose-embedded confidence vocabulary (**confirmed** / **single-sourced** / **candidate** / **assumed**) — the heavier research-audit apparatus (evidence-coverage tables, validation-gap Q&A, traceability registers, emoji evidence tags) stays in the `assets/` backing files as working research, not duplicated into the narrative the reader actually submits | 2026-09-13 (final consolidation) |
| D-36 | PDF output is generated via pandoc→HTML→Chrome-headless-print-to-pdf with a custom stylesheet, not pandoc+tectonic LaTeX, specifically to get CSS-level control over margins and image sizing that LaTeX's float/spacing model made painful throughout this project (Mistakes #6-9, #12) — docx generation is unaffected and still goes through plain pandoc | 2026-09-13 (final consolidation) |

---

## 8. MISTAKES & ERRORS LOG (technical)

| # | Mistake | Root cause | Fix | Session |
|---|---|---|---|---|
| 1 | Stray unpopulated TOC heading/link block rendered into early PDFs | Word TOC field flattened to plain text by pandoc's docx reader | Stripped before typesetting | 2026-09-06 |
| 2 | Dropped Unicode "interrupted relationship" glyph in a diagram legend | LaTeX/font couldn't render the glyph | Replaced with LaTeX-safe "X" | 2026-09-06 |
| 3 | Duplicate "Onion Proximity Map" caption on a manually-added infographic | Caption inherited from the image above it during editing | Relabeled to accurate caption | 2026-09-06 |
| 4 | Leverage-quadrant chart title clipped | Mermaid `quadrantChart` title too long for its rendering | Shortened title, regenerated | 2026-09-06 |
| 5 | Provisional-timeline diagram sliced across a page break | Diagram too tall for one page in vertical layout | Rebuilt as horizontal (`LR`) layout | 2026-09-06 |
| 6 | Regenerated PDF was 11 pages vs. committed original's 8, with more LaTeX overfull-hbox warnings | Naive `pandoc --pdf-engine=tectonic` doesn't replicate whatever image-fitting the original session used (never saved as a script) | Investigated via page-image rendering; built explicit image-fit header (see #7 below) | 2026-09-12 |
| 7 | Flowchart diagram clipped off the bottom of its page, caption/page-number missing | LaTeX's default `\includegraphics` doesn't constrain to remaining page space | `adjustbox`-based `\includegraphics` override with `max width`/`max height`/`keepaspectratio` | 2026-09-12 |
| 8 | `adjustbox` header errored: `'max width' undefined in family 'Gin'` | Plain `\includegraphics` doesn't understand adjustbox keys without rerouting | Redefined `\includegraphics` to call `\adjincludegraphics` | 2026-09-12 |
| 9 | Image still overflowed after adjustbox was wired in, no visible change | Explicit `height=` from the original pandoc call came *after* `max height=` in the merged key list, silently overriding it (LaTeX key-value is left-to-right) | Reordered keys so constraints come after the pass-through of original keys | 2026-09-12 |
| 10 | Word TOC showed page "1" for every single entry | Word TOC field never refreshed ("Update Field") before the docx was saved; pandoc flattens the frozen field text as-is | Stripped the stale block; regenerated via pandoc's native `--toc` on cleaned Markdown | 2026-09-12 |
| 11 | Extracted images failed to resolve (`Could not fetch resource media/image1.png`) | `--extract-media=DIR` nests files one level deeper (`DIR/media/media/...`) than the markdown's own relative reference expects | Set `--resource-path=DIR/media` instead of `DIR` | 2026-09-12 |
| 12 | Figure 2 drifted from page 4 to page 7, disconnected from its introducing paragraph | Going through a Markdown intermediate causes pandoc to emit standard floating `figure` environments, which LaTeX is free to place wherever it finds room | Added `\usepackage{float}` + `\floatplacement{figure}{H}` to force in-place rendering | 2026-09-12 |
| 13 | "Bottlenecks:"/"Tensions:" bullet lists rendered as literal inline `-` characters in one run-on paragraph instead of an actual bulleted list | No blank line between a bold "label:" line and the `- item` list that follows it — CommonMark/pandoc lazy-continuation treats them as one paragraph | Inserted the missing blank line before each list | 2026-09-12 (Task 6 completion pass) |
| 14 (flagged, not fixed — belongs to Task 7) | `report/figures/d10-loop-r1.mmd`'s "financial pain of no-show" → "pressure to fix billing" link is signed `-`, which reads as more pain causing less pressure to fix — likely a polarity error | Not root-caused this pass (out of scope); possibly just a mislabeled sign when the diagram was first drawn | Not fixed — flagged in Section 9 for whoever next touches Task 7 | 2026-09-12 (Task 6 completion pass) |
| 15 | Every icon in every `chip()` node rendered as a barely-visible sliver instead of a centered icon | The `<use>` element's x/y offset was computed from the wrong reference point (`left+10-9`) instead of the badge circle's actual center | Recomputed the badge center explicitly (`left+icon_r+10`) and offset the icon from that | 2026-09-12 (Task 6 visual redesign) |
| 16 | Every "intended, but not confirmed/observed" label on a broken CLD link silently disappeared from the rendered PNG — only its polarity sign showed | The edge tuple's `label` field was unpacked in `loop_diagram()` but never passed into the `curved_arrow()` call | Added the missing `label=label` argument; confirmed by grepping the intermediate HTML for the expected string before and after | 2026-09-12 (Task 6 visual redesign) |
| 17 | Two edges sharing an endpoint in a triangular/polygonal loop diagram computed near-identical label/sign positions, so one visually occluded the other | Both anchored their label/sign at the straight-line midpoint between the two nodes, which coincides for edges sharing a node in a symmetric layout | Moved the anchor to a point along the actual bezier curve biased toward the source node (`t≈0.32`) in `curved_arrow()`, a general fix not a one-off | 2026-09-12 (Task 6 visual redesign) |
| 18 | New intra-zone connector labels rendered on top of, and obscured, a neighboring node's text; separately, one connector's arrow flew past its intended target and off the canvas | Label boxes don't fit in the narrow gap between closely-spaced adjacent chips; and moving an edge's *endpoint* coordinate to dodge an obstacle moves where the arrowhead lands, not just the path | Dropped labels on short adjacent connections; for routing around obstacles, kept the endpoint fixed at the real target and used `bend` (which only moves the bezier control point) instead | 2026-09-12 (Figure 2/4 follow-up) |
| 19 | Three "NO FORMAL CHANNEL" arrows shot off the top of the canvas entirely | `curved_arrow`'s `bend` is a fraction of the edge's own length; for a long, nearly-horizontal edge the perpendicular ("bend") direction is nearly vertical, so a bend fraction that looks like a gentle arc on a short/steep edge becomes a huge absolute pixel displacement on a long/flat one | Used much smaller bend magnitudes (`-0.06` to `-0.15`) sized to the edge's actual length and orientation, not a fraction copied from unrelated edges | 2026-09-12 (Figure 2/4 follow-up) |
| 20 | A green "already fired once" precedent badge in the governance diagram rendered its text on top of, and overlapping, the "CDS Committee" node it was meant to annotate | The badge had no bounding box of its own — just floating text positioned near the node, close enough to collide with it | Gave the badge its own bounded box (rect + icon + text) positioned clearly beside, not overlapping, the node | 2026-09-13 |
| 21 | An unexplained green blob rendered floating off the top edge of the Kitchen and Supply Subsystem diagram's canvas | A copy-paste typo passed the literal string `"1170+80"` as an SVG `cy` coordinate instead of a computed numeric value | Removed the broken, actually-unused element entirely; caught by looking at the rendered PNG, not by reading the generator source | 2026-09-13 |
| 22 | A bold `**label:**` line immediately followed by a `- ` list (Outstanding Data Collection's "in priority order" list) collapsed into one run-on paragraph with literal inline dashes — the exact same defect class as Mistake #13 | No blank line between the bold lead-in and the list — CommonMark/pandoc lazy-continuation treats them as one paragraph | Inserted the missing blank line; **this is the second occurrence of this exact mistake in this project** — worth proactively grepping a new document for "bold label line immediately followed by a `- ` line" rather than relying on catching it during page-by-page review | 2026-09-13 |
| 23 | The "Merge Prathyusha's braindump #2; deepen Task 6..." session log entry's own `### Session:` header line went missing from `MASTER_CONTEXT.md` at some point across this conversation's several `Edit` calls that inserted new session entries immediately before it, leaving its body text orphaned directly under the previous session's content with no heading of its own | An `old_string`/`new_string` edit that re-appended "the same heading" verbatim as part of relocating a new entry in front of an existing one is fragile — a later edit targeting nearby text can end up matching/consuming that re-appended heading without it being obvious from the diff alone | Found by manually tracing session headers with `grep -n "^### Session"` and noticing a body paragraph with no preceding header at all; restored the missing header from the paragraph's own content and date | 2026-09-13 |
| 24 | `chip()`'s second text line rendered illegibly (dark grey on dark navy) on the first draft of the new Iceberg Model diagram's mental-models band | `chip()` hardcodes the second line's fill color instead of deriving it from the `text_color` parameter, so a dark custom `fill` with `text_color="white"` still gets a dark second line | Switched those three chips to a light fill instead of patching the shared function (other diagrams depend on its current behavior); caught by rendering and reading the PNG, not by reading the generator source | 2026-09-13 |

**Non-technical/process "mistakes" worth naming (source-data and research hygiene, not pipeline bugs):**
- A source-data labeling collision in the raw interview transcripts (two different files both labeling a respondent "Daily eater - 2") was caught by cross-transcription comparison, not assumed away (D-11).
- The "Mess Council" stakeholder was initially treated as a confirmed IIIT-H actor before independent verification revealed it was very likely a naming mix-up with a different institution (IIT Hyderabad) — corrected once checked against source, not left as an unverified assumption (2026-09-06 audit session).
- "Sleeping collective leverage" was stated as a finding, then overturned by a later-discovered precedent and relabeled "exercised-but-unmeasured leverage" rather than quietly edited without explanation (2026-09-06).
- A source-photo transcription glossed one line as "portal above" instead of transcribing it verbatim, which is exactly how a real inconsistency on the source poster itself (two different portal domains on one sign) went unnoticed until the source image was re-opened directly and read again (2026-09-13) — a concrete argument for the project's own "verbatim, not paraphrased" transcription standard, not just a stylistic preference.

---

## 9. OPEN ITEMS (as of 2026-09-13, updated after Prathyusha's braindump #2 AND this session's Task 6 reconciliation of it — check before assuming any of these are still open)

- **Mess-committee / vendor-side interview has not been conducted; is now specifically "pending institutional approval."** Every deliverable that touches governance or kitchen operations (Tasks 3, 4, 5, and now 6, 7) says so in its "Outstanding Data Collection" section — Phase 2's wording is more specific than Phase 1's ("will be collected and submitted later"), confirming it's the same single pending interview referenced everywhere, not several separate gaps. **Narrowed but not closed, 2026-09-13:** a CFS Chair interview (`prathyusha_braindump_2.md`, see the 2026-09-13 session entries below) answers a real slice of both blocked claims from an adjacent angle — (a) stock assessment is weekly and consumption-pattern-driven (high-turnout items like paneer/egg/chicken tracked at >90% turnout; general turnout ~70%; breakfast specifically 35-40%), and (b) a T-4-day registration→vendor-communication cadence is now confirmed directly, not just inferred from the student-facing portal. **Still open:** this was the CFS Chair, one level up from CDS; the CDS Chair (**Giri**) has not been interviewed directly, and the exact mechanism connecting weekly stock assessment to per-item preparation quantity (i.e. whether it's truly calibrated to the low breakfast-specific attendance figure, or to a peak-hour assumption) is still not confirmed — the original Task 7 candidate-loop question survives, just with better surrounding context.
- **~~Phase 2 has no `.docx`/`.pdf` yet~~ — fully resolved, 2026-09-13 (final consolidation).** Both `System Map.docx`/`.pdf` (25pp) and `Systemic Problem Analysis.docx`/`.pdf` (11pp, new title, replacing the old "not yet built" status) are current, built via the new HTML+CSS+Chrome pipeline (Section 5.7), and verified page-by-page via `pdftoppm`.
- **~~Task 7 consolidated CLD had Mermaid source only~~ — resolved.** The polished `Systemic Problem Analysis.md`/`.docx`/`.pdf` now uses real SVG diagrams throughout (a compact 8-loop dashboard, `diagram17-loop-overview-dashboard.png`, plus full individual loop diagrams for B4/R1/B3), not a Mermaid chart. The full Mermaid-sourced six-loop composite in `assets/task7/02_causal_loop_diagram.md` still exists as backing detail but is no longer what ships in the polished document.
- **~~Two Task 7 files coexisted~~ — resolved, 2026-09-13 (final consolidation, D-34).** `prathyusha_task7.md` was merged into `07-systemic-problem-analysis.md` and deleted. There is now exactly one Task 7 markdown/docx/PDF set, titled "Systemic Problem Analysis," combining Neha's original findings with the full rebuild.
- **~~Task 7's two highest-priority validation gaps~~ — resolved directly by the user, 2026-09-13.** (1) The CFS Chair's awareness of the gap (a further tentative ~45% figure, alongside 35-40%) has been confirmed to have **never** reached a registration/billing-policy discussion. (2) Skip Meal declarations **do** reach the kitchen for same-week prep-quantity forecasting, distinct from the still-rigid T-4 sourcing lock.
- **Replacement P0 priorities, full list in `assets/task7/08_validation_gaps.md`:** (1) why Skip Meal *uptake* is reported near-zero despite the mechanism now confirmed to work; (2) whether the vendor's contract terms would allow a partial/permanent version of the shock-adaptive model (loop B4) — the single most consequential open question for any future intervention design, though out of scope for this diagnosis-only document.
- **A large batch of 🔷 ASSUMED / "assumed" claims across both `06-system-map.md` and `07-systemic-problem-analysis.md` are, by design, not yet interview-confirmed:** the Canine Council scrap-feeding mechanism and its dog-population/foot-traffic effects; whether faculty or early-shift staff consume mess food in any counted or uncounted volume; the vendor-margin/quality-tradeoff mechanism (loop R5); whether the standing model's persistence reflects vendor-interest (RC5) as well as fragmented decision rights (RC3); and the exact reason the shock-adaptive model (B4) is retired once its trigger passes rather than partially retained. Full-rigor backing with explicit evidence tags for all of this still lives in `assets/task7/*.md` even though the polished document no longer shows the tagging apparatus itself — check there before treating any of these as more certain than "assumed."
- **An "Extended System Boundary" overview diagram (all newly mapped subsystems around the core registration/billing/kitchen system) and a dedicated waste/Canine-Council subsystem diagram (parallel to `diagram13-kitchen-supply-subsystem.png`) were discussed but still not built** — three new diagrams were built this pass instead (Iceberg Model, Root Cause Network, loop dashboard), prioritized because the user asked for them by name. These two remain the natural next visuals if more depth is wanted.
- **The exact scope of the CFS Chair's "35-40% / ~45%" breakfast turnout figures is unconfirmed** (all four messes, or Kadamba only; over what time window; whether the two figures are the same measurement) — flagged in `08_validation_gaps.md` P1-1, relevant to how confidently either document generalizes it.
- **A likely CLD polarity error in `report/figures/d10-loop-r1.mmd`** (and possibly `d11-loop-b1.mmd` — not checked): the "financial pain of no-show" → "pressure to fix billing" link is signed `-`, which reads as more pain causing less pressure to fix. Flagged during the Task 6 completion pass but deliberately not fixed (out of scope, belongs to Task 7 / whoever owns that file). See Mistake #14.
- **The R2 candidate loop (energy-crash/compensatory-purchase cycle, from the original puzzle brief) is unconfirmed beyond its first link.** A single added interview question — what a respondent does in the hours after skipping breakfast, and whether it changes their evening routine — would confirm or kill it. Does not require a new interview round or institutional approval; can be asked of the existing six respondents or added to any remaining Guide 1 interviews.
- **Direct observation of a live breakfast service window has not been conducted**, despite a 30-minute-interval observation protocol (7:15/7:30 AM setup through post-close) being written and ready to run. Would resolve the unconfirmed exact shape/timing of the crowd-peak curve.
- **The true respondent count behind "Daily eater - 2" is unresolved** (see Mistake/finding above) — either a mislabeling or a genuinely missing sixth transcript; unverifiable without the original interview source re-checked directly with whoever conducted it.
- **The relationship between "Mess Council" and the Mess Committee remains unresolved**, compounded by the likelihood that "Mess Council" is a cross-institution naming mix-up rather than a real IIIT-H body — needs direct confirmation, not further inference.
- **The subject and outcome of the historically-referenced "organized student email effort"** is still the single highest-priority open governance question per the 2026-09-06 audit, with only a strong-but-unconfirmed candidate match found so far.
- **Whether the ~30% registration-to-attendance gap figure is accurate** is unverified — cited by two respondents but not confirmed against records.
- **No single population-level attendance trend exists** across the six real respondents (some rising, some falling, one flat at zero) — any future claim that "the paradox is worsening/improving" needs to specify for whom, not assert it generally. This is a standing caution for anyone extending this research, not a to-do item to resolve.
- **`report/` (the separate LaTeX write-up) has its own figure set** (`report/figures/`) that overlaps conceptually but is a distinct copy from `deliverables/phase-1/assets/` — if a diagram is corrected in one location, check whether the other copy needs the same correction; there is currently no automated link between them.
- **~~This `MASTER_CONTEXT.md` file itself needs to be committed and pushed~~ — done** (commit `882cc72`, 2026-09-12). Resolved.
- **Before pulling/pushing in future sessions, always check `origin/main` for teammate commits first** (`git fetch` + compare `git log -1` local vs. `origin/main`) — this session found a full day-old teammate commit (`b7c69ac`, Phase 2 kickoff) sitting unpulled on the remote simply because nothing had triggered a fetch since it landed. There is no other signal that surfaces a teammate's push automatically in this workflow.
- **~~Prathyusha's braindump #2 content has not yet been reconciled into the numbered deliverables~~ — Tasks 6 and 7 both resolved, 2026-09-13.** `06-system-map.md` and `07-systemic-problem-analysis.md` both now carry the CFS/CDS org structure, the escalation loop (B3/R3), the kitchen/supply subsystem, dining-hall and pricing tables, and the Bakul/Palash correction. **Still not reconciled:** the Stakeholder Map / Power-Interest/Leverage Map (the CFS/CDS/Warden terminology-overlap questions below affect those documents directly, not just Tasks 6-7); `01-system-context-brief.md` (still thin on the real capacity/pricing numbers now available); Task 4/Process Trace (the Kadamba kitchen equipment walkthrough is the first real physical/spatial account of a mess kitchen in this project and hasn't been folded in there yet).
- **New, 2026-09-13 — four open terminology-reconciliation questions, none resolved, none merged/assumed:** (1) does "CDS" name the same operations department Phase 1 called "Mess Office"? (2) is "CDS Committee" the same body as "Mess Committee"? (3) is "CDS Student Council" the same body as (or a subgroup of) "Student Parliament"? (4) is "Warden" (Phase 1's named owner of vendor/structural matters) actually CFS itself (Campus *Facilities* Services, which funds infrastructure directly)? All four are visually flagged (dashed grey, "unconfirmed") in `deliverables/phase-2/assets/diagram12-governance-escalation.png` and in `06-system-map.md`'s text, per D-24. Resolving these needs a direct question to the CFS Chair, Giri (CDS Chair, not yet interviewed at all), or both — not inference from either research session's existing notes.
- **New, 2026-09-13 — the Kadamba "420 seats vs. 1,200 capacity" figures are not reconciled**, and are presented in `06-system-map.md` as a likely (not confirmed) difference in what "capacity" means (registration/allocation vs. physical seats) rather than a contradiction. Confirm directly before either number is treated as settled in any other deliverable.
- **New, 2026-09-13 — whether the open-tender catering contract is fixed-price or cost-passthrough is unconfirmed** and determines whether the project's earlier LPG-price → menu-shrinkage hypothesis (braindump #1) is still mechanically active under the current outsourced-catering model.
- **New, 2026-09-13 — the R3 "menu transparency backfire" candidate loop needs exactly one added interview question** (does knowing next week's menu change whether a respondent registers or shows up?), answerable with the existing six respondents — no new interview round needed. Same standing ask as the still-open R2 question (what a respondent does in the hours after skipping breakfast).
- **New, 2026-09-13 — repo path discrepancy noticed, not resolved.** This session's environment reported the repo checked out at `/Users/klprathyusha/Sem 3/Systems thinking/Project/breakfast-paradox-systems-thinking`, which does not match the path recorded in Section 1 (`/Users/Shared/Files From e.localized/PDM/Sem3/Systems Thinking/Project`). Both may be valid (different machines/accounts sharing the same GitHub remote), but this wasn't confirmed with the user this session — worth checking before treating either path as canonical, especially if a future session can't find a file "Section 1 says" should be there.
